# Continue Here — Session 79 Handoff
_Generated: 2026-06-09_
_Version at pause: 1.35.26_
_Last commit: 9a0873c_

---

## What Was Done This Session

### 5 adversarial bugs fixed from session 78 evaluation

All P0/P1/P2 bugs from the 10-bug adversarial report resolved and regression-tested.

**F-001 — @PreAuthorize expression truncation (P0) ✅ FIXED**
- Root cause: `_ANN_WITH_ARGS_RE = re.compile(r'^(@[\w.]+)\s*(?:\(([^)]*)\))?')` — `[^)]*` stopped at first `)` inside annotation args
- Fix: `repository_ir.py:108` — quoted-string-aware pattern handles nested parens inside `"..."` and `'...'`
- Before: `"expression": "hasRole('USER'"` (truncated)
- After: `"expression": "hasRole('USER')"` (complete)
- Tests: 7 new in `TestAnnotationArgsCapture`

**F-002 — Maven property substitution not resolved (P1) ✅ FIXED**
- Root cause: `_scan_dep_file` applied dep rule patterns to raw pom text; `${hibernate.version}` not substituted
- Fix: `migrate_check.py` — added `_resolve_maven_properties()` pre-processor (3-pass, before pattern matching)
- Tests: 6 new in `TestMavenPropertyResolution`

**F-003 — Multi-module pom inflates dep findings (P1) ✅ FIXED**
- Root cause: EhCache×3, SpringFox×2 across parent+child poms → inflated effort_days
- Fix: `migrate_check.py:run_migrate_check()` — dedup dep findings by `rule_id`; keep first (root pom)
- Tests: 4 new in `TestMultiModuleDepDeduplication`

**F-008 — XML security opacity (P2) ✅ FIXED**
- Root cause: `extract_java_endpoints()` only detected Java `@EnableWebSecurity`; XML Spring Security invisible
- Fix: `repository_ir.py:3311-3355` — XML namespace detection via regex; sets `security_model=xml_or_filter_chain`; re-tags `none_detected` endpoints
- Tests: 6 new in `TestXmlSecurityOpacity`

**F-009 — Sync @EventListener inside @Transactional not flagged (P2) ✅ FIXED**
- Root cause: `_compute_event_risk` only flagged BEFORE_COMMIT; plain @EventListener in TX ignored
- Fix: `spring_event_topology.py` — detects `tx_publishers`, adds `sync_in_tx_risks` to `transaction_context`; `_compute_event_risk(sync_in_tx_count=...)` → `high`
- Tests: 6 new in `TestSyncEventListenerInTxRisk`

**F-004 — Constructor-injected deps in outgoing_deps (P2) ✅ CONFIRMED OK**
- Reproduction showed InjectionGraph captures constructor injection correctly
- Root issue was test used `file_paths=[]`; with `find_java_files()` it works
- No code change needed

**Final test count:** 2345 → 2367 passed (+22 regression), 0 failures, 3 skipped (pre-existing)

---

## Remaining Bugs (not addressed)

| ID | Severity | Description | Location |
|----|----------|-------------|----------|
| F-005 | LOW (P3) | `impact` direct_callers includes `OrderController#<init>` noise | caller filter |
| F-006 | LOW (P3) | `explain` entity description says "no persistence annotation detected" despite `@Entity` | explanation template |
| F-007 | MEDIUM (P2) | `cold-start --compact` returns 78 tokens vs advertised "~10K tokens" | cold-start content floor |
| F-010 | LOW (P3) | MCP status shows external server warning with no automated fix path | mcp status UX |

---

## Next (priority order)

### 1. `migrate-check --target` filter flag (P0 — session 76 backlog)
- `sourcecode migrate-check . --target java17|java21|springboot3`
- Filter `_ALL_RULES + _XML_RULES + _DEP_RULES` by `rule.migration_target`
- File: `cli.py` — `migrate_check_cmd` function + `run_migrate_check(path, ..., target_filter=None)`
- Users doing SB3-only migration don't want Java 9+ encapsulation noise

### 2. `migrate-check --ci` flag (P1 — session 77 backlog)
- Exit code 1 when `blocking_count > 0`
- Same pattern as spring-audit `--ci` / `--no-ci` bool flag (Typer 0.24+ compatible)
- File: `cli.py` — `migrate_check_cmd`

### 3. F-007: cold-start --compact content floor (P2)
- `sourcecode cold-start . --compact` on small repo returns only 78 tokens
- README advertises "~10K tokens for typical project"
- Fix: enforce minimum content floor — always include key sections even for small repos

### 4. F-005: caller filter (P3)
- `#<init>` constructors in direct_callers are noise — callers should be methods, not constructors

### 5. F-006: @Entity detection in explain (P3)
- Entity description template says "no persistence annotation detected" despite `@Entity` being present

---

## Known Flaky Tests (pre-existing, always exclude)

```bash
pytest tests/ -x -q \
  --ignore=tests/test_output_ux.py \
  --ignore=tests/test_task_differentiation.py \
  --ignore=tests/test_signal_hierarchy.py
```

---

## Resume

```bash
source .venv/bin/activate
sourcecode --version  # 1.35.26
pytest tests/test_migrate_check.py -q  # 172 pass
```

**Start with:** `migrate-check --target` filter flag (P0).
Find the CLI entry point: `grep -n "migrate.check\|migrate_check_cmd" src/sourcecode/cli.py | head -10`
