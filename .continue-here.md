---
context: default
phase: null
task: null
total_tasks: null
status: paused
last_updated: 2026-05-29T21:00:00Z
---

# Session 36 — 1.33.4 Bug Fixes (7 contract/observability issues)

<current_state>
Session 36 complete. 1.33.4 committed at 6480eae. 1655 tests pass, 3 skipped.
All source bugs fixed. E2E verification still pending (requires normal Terminal for sync_to_pipx.sh).
</current_state>

<completed_work>

- Bug 1: `--changed-only` untracked files — new files now included; clean-repo JSON emits `analysis_scope`+`_meta`
- Bug 2: `cache clear --all` — `--all` canonical flag, `--include-ris` hidden compat alias
- Bug 3: RIS cold-start validation block — `validation.{spring_detected, controllers_found, endpoints_found, status}`; Spring+no-endpoints → `cold_start_incomplete`
- Bug 4: `--env-map` unhidden from CLI help
- Bug 5: `fix-bug` no-symptom warning — stderr warning when `--symptom` omitted
- Bug 6: `generate-tests` output split — `untested_sources` canonical + `test_gaps` compat + `existing_test_count`
- Bug 7: LQN-02 test fix — assertion updated for current description string
- Version bump: 1.33.3 → 1.33.4

All committed: `6480eae fix: 7 contract/observability bugs — bump 1.33.3 → 1.33.4`
</completed_work>

<remaining_work>

- **P1 — Live E2E test**: Run `bash /private/tmp/sync_to_pipx.sh` from normal Terminal (EPERM in Claude Code shell), then verify:
  - `sourcecode cold-start /private/tmp/spring-petclinic` → check `validation` block
  - `sourcecode cache clear /private/tmp/keycloak --yes` → confirm `--all` flag works
  - `sourcecode fix-bug /private/tmp/keycloak` → confirm no-symptom stderr warning
  - `sourcecode prepare-context generate-tests .` → check `untested_sources` + `existing_test_count`
- **P2 — Publish**: PyPI + Homebrew tap update for 1.33.4
- **P4 — Deferred**: suggest_entrypoint(task), MCP for Claude Code, RIS watch mode
- **Cleanup**: `rm src/sourcecode/cache.tmp_new`; decide fate of `docs/PRODUCT_AUDIT.md`
</remaining_work>

<decisions_made>

- `cold_start_incomplete` status introduced: Spring repo with controllers but no endpoint index is NOT ready — agents must call get_endpoints or rebuild.
- `untested_sources` added as canonical alias, `test_gaps` kept for backward compat — no breaking rename.
- `--all` chosen over keeping `--include-ris` as primary because it's more discoverable.
</decisions_made>

<blockers>

- Live E2E verification requires `bash /private/tmp/sync_to_pipx.sh` from normal Terminal (Claude Code shell has EPERM on workspace file renames).
</blockers>

## Infrastructure State
- Version: 1.33.4 (committed, not published)
- Tests: 1655 passed, 3 skipped
- pipx install: still on 1.33.3 (sync pending)
- Untracked: `docs/PRODUCT_AUDIT.md`, `src/sourcecode/cache.tmp_new`

<context>
Session 36 addressed the highest-priority bugs from the v1.33.x stability audit. Strategic direction: sourcecode is a "persistent architectural context layer for AI agents on Java/Spring repos." The RIS validation block is the most impactful change — it prevents agents from trusting incomplete snapshots and silently producing wrong answers. Next priority after E2E verification is suggest_entrypoint (task→candidate files mapping), which multiplies agentic workflow productivity.
</context>

<next_action>
Run `bash /private/tmp/sync_to_pipx.sh` from normal Terminal, then E2E verify 1.33.4 fixes above. Then publish to PyPI.
</next_action>
