---
session: 70
date: 2026-06-05
status: paused
type: feat — explain command (class architectural summary)
---

# Continue Here — Session 70

## What was done

Implemented `sourcecode explain <ClassName>` command. Generates human-readable
architectural summary of a class from existing static analysis — no new parsers.

### New files
- `src/sourcecode/explain.py` — engine module (`explain_class()`, `ClassExplanation`)
- `tests/test_explain.py` — 22 tests EXP-01..EXP-22

### Modified files
- `src/sourcecode/cli.py` — `explain` command registered + added to `_SUBCOMMANDS`
- `src/sourcecode/__init__.py` — v1.35.13 → **v1.35.14**
- `pyproject.toml` — v1.35.13 → **v1.35.14**

### Command
```
sourcecode explain UserService
sourcecode explain OrderController /path/to/repo
sourcecode explain UserService --format json
sourcecode explain UserService --output out.md --copy
```

### Output sections
1. Purpose (stereotype + inheritance + class-level TX)
2. Public Methods (from raw_ir nodes)
3. Used By — incoming callers (DI dependents + reverse graph)
4. Calls — outgoing deps (injection graph)
5. Publishes — events published (EventGraph)
6. Consumes — events consumed (EventGraph)
7. Transactions — @Transactional boundaries (method + class level)
8. Security — @PreAuthorize etc. (security_index + method annotations)
9. REST Endpoints (EndpointIndex)

### Test results
**2169 passed, 3 skipped** (was 2147). Last commit: `9cddb96`

---

## Carried-over open items (priority order)

### HIGH
1. **Regression tests for session-64 BUG-001/002/003** (still pending)
   - `tests/test_spring_tx_analyzer.py`, `tests/test_repository_ir.py`, `tests/test_spring_impact.py`

### MEDIUM
2. **Fix direct_callers dedup** (`spring_impact.py` `_bfs_callers()`)
   - Field node (`Class.fieldName`) + class node (`Class`) both appear in direct_callers
   - Confirmed in openmrs (AllergyValidator) + petclinic (VetController)

3. **Fix event topology limitations text** (`spring_event_topology.py`)
   - Add: "and ApplicationListener<T> interface implementations"

4. **`endpoints_affected: []` in BroadleafCommerce impact-chain**
   - OrderServiceImpl: 72 callers, 110 endpoints exist — chain from HTTP controller not resolved

### LOW
5. MCP documentation — 28 tools, 0 documented
6. `docs/schema.md` — missing spring-audit/impact-chain schemas
7. `CHANGELOG.md` — doesn't exist
8. `server.py` dead code (~750 lines dead `@mcp.tool()` defs)
9. `--no-cache` not in subcommands (endpoints, spring-audit, impact-chain)

---

## GSD position

- Phase 13 (inferencia-arquitectonica): **COMPLETE**
- **Phase 14 (Flujo de Ejecucion): NEXT** — 0 plans
  - Goal: `--execution-flow` flag, HTTP request tracing, pipeline detection, critical path
  - Requirements: EXEC-01..EXEC-04

## Immediate next actions (choose)

**A) GSD Phase 14** → `/gsd:discuss-phase` or `/gsd:plan-phase`
**B) Session-64 regression tests** → `test_spring_tx_analyzer.py` / `test_repository_ir.py` / `test_spring_impact.py`
**C) Fix direct_callers dedup** → `spring_impact.py:_bfs_callers()`
