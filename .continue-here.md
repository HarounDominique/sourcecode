---
context: default
phase: none (ad-hoc field-test fix — v1.67.0 shipped)
status: paused
last_updated: 2026-06-30T18:41:02Z
last_commit: 53dde5a
---

# Continue Here — sourcecode

**Paused:** 2026-06-30
**Branch:** master (pushed: `b9f22ce..53dde5a`)
**Last commit:** 53dde5a — `fix(framing): credibility on non-Spring repos (OFBiz field test) — v1.67.0`
**Working tree:** clean

<current_state>
v1.67.0 shipped and pushed. Ad-hoc field test on Apache OFBiz (non-Spring, own
framework) exposed 7 high-confidence false-framing defects; all fixed, verified on
the real repo, regression-tested, and validated across 10 diverse repos. No work in
flight — clean stopping point.
</current_state>

<completed_work>
v1.67.0 — "framing accuracy on non-Spring repos" (all 7 defects, committed 53dde5a):
1. spring-test no longer implies runtime Spring — scope/artifact-aware
   `_detect_spring_usage`; `spring_present` vs `spring_test_only` (migrate_check.py)
2. phantom boot3 0/100 gone — N/A without runtime Spring, excluded from aggregate
3. applicability gating — jakarta gated on evidence (migratable javax.* OR jakarta.*
   adopted); empty aggregate → `readiness_score: null` (was manufactured 100)
4. javax.servlet kept on Jakarta-EE/Tomcat10 axis, not mislabeled Spring Boot 3
5. modernize dead zones → `statically_unreferenced` + `framework_dispatched`
   (`_partition_static_unreferenced` in cli.py: signature allowlist + XML/SPI scan)
6. gradle deps 0→59 — string-aware `_strip_gradle_comments` (glob '**/*.java' no
   longer eats the dependencies block); gap reported when nothing resolves
7. vendored web libs (jquery-*.js, bootstrap.*) excluded — path_filters.is_vendor_path
8. `web_mvc` label vendor-neutral ("Server-side MVC web app") — context_summarizer.py
   + summarizer.py

Tests: new golden `tests/test_ofbiz_framing_regression.py` (9). Updated empty-repo /
non-Java readiness expectations (100→None) in test_migrate_check, test_spring_audit_cli,
test_enterprise_benchmarks + MCP docstrings. **2872 pass.**
CHANGELOG + version bump (1.66.0→1.67.0) + memory finding recorded.

Also: docs/MANUAL-USUARIO.md committed; docs/PLAYBOOK_V2.md gitignored (NOT distributed).
</completed_work>

<remaining_work>
None for this task. Possible follow-ups (not started, optional):
- eureka project_type=unknown (Netflix/Jersey not in framework detectors) — could add
  Jersey/Governator detection.
- blocking_count includes jdk_modernization highs while readiness excludes them
  (jobrunr: readiness 100 + 14 "blockers") — pre-existing semantic; consider a
  migration-only blocker count.
</remaining_work>

<decisions_made>
- Principle: degrade to N/A, never a manufactured 0 OR 100; declare gaps not certainty.
- jakarta applicable = migratable javax.* findings OR jakarta.* namespace adopted —
  distinguishes already-migrated Jakarta repo (applicable, 100) from never-JavaEE repo
  (N/A). Keeps Keycloak golden at 100, OFBiz honest on real servlet surface.
- spring-test artifact treated as test regardless of declared scope (OFBiz declares it
  under legacy `compile`).
- Dead-zone detector reframed (never "dead" — `statically_unreferenced`) + config-name
  scan, because static call-graph can't see framework dispatch.
</decisions_made>

<blockers>
None.
</blockers>

## Infrastructure State
- Field-test repos at `/Users/user/Documents/workspace/testing/` (ofbiz-framework,
  keycloak, BroadleafCommerce, eureka, alfresco-community-repo, jobrunr, openmrs-core,
  examples, spaghetti-api, spring-petclinic).
- Tests: use system `/opt/homebrew/bin/python3` (.venv python SIGKILLs).
- Cached `--compact`/`migrate-check` can serve stale — use `--no-cache`.

## Critical Anti-Patterns (do NOT repeat)
- PRE-EXISTING FAILING TEST (advisory): `test_file_chunker.py::TestRealLargeFile::
  test_no_timeout_on_7500_lines` fails with or without changes — external OFBiz file is
  now 6476 lines vs hardcoded ≥7500. Deselect when running the suite; it is NOT a
  regression.

<context>
Task complete and shipped. Nothing in flight. Validation sweep over 10 repos confirmed
no regressions and zero false-Spring claims.
</context>

<next_action>
Nothing pending. If resuming for new work, start fresh. Optional follow-ups listed in
<remaining_work>.
</next_action>
