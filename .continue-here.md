---
context: default
phase: none (post-milestone — all 13 GSD phases 100%)
status: paused
last_updated: 2026-06-14T11:48:21Z
---

# Continue Here — Hybrid Monetization Model (Session 96)

**Paused:** 2026-06-14
**Work:** Rework Free/Pro from per-command gating to a size + automation axis. Sync README, --help, PRODUCT_TIERS.md, and the landing page. Version bump.
**Status:** Code complete and committed (3 commits across 2 repos). One blocking external action pending (Lemon Squeezy price).

---

## Current State

Ad-hoc post-milestone work — not a GSD phase. All code done, tested (2516 passed), committed to `master` in both repos. Nothing pushed. The only open item is external: the live checkout still charges the old price.

---

## Completed This Session

| # | Commit | Repo | Summary |
|---|--------|------|---------|
| 1 | `1ade205` | atlas-cli | Hybrid size-based gating: `is_large_repo` / `require_repo_or_pro` in `license.py`; 8 cli.py gate sites converted capability→size; delta keeps 30-run quota; PRODUCT_TIERS.md + README pricing rewritten |
| 2 | `a0016f6` | sourcecode-landing | Pricing cards reframed; €29.99→€19/mo (+€190/yr per dev); MCP marked Free; `[Pro]`→`[Free ≤500 Java]`; EN+ES i18n; success.html |
| 3 | `39d2706` | atlas-cli | Version bump 1.35.35 (pyproject, __init__.py, README) |

Model in one line: **every command free on repos ≤500 Java files; Pro unlocks the same commands on enterprise monoliths + unlimited CI/CD delta; non-Java free at any size; local MCP free.**

## Remaining Work

None in code. Two follow-ups (one blocking, external):

## Blockers / Human Actions Pending

- [ ] **BLOCKING (external):** Lemon Squeezy product still priced €29.99. Landing displays €19. Update product to €19/mo + create €190/yr annual variant + checkout link before go-live. Current checkout `buy/99c8675f` charges the old price.
- [ ] Push commits for both repos (atlas-cli + sourcecode-landing on `master`).

## Decisions Made (with rationale)

- **Gate by size (>500 Java files) + automation (delta), not capability** — gating core capability churns users; gating scale ~doubles conversion (SaaS data).
- **Java-only monetization** — non-Java repos free at any size, by design. `is_large_repo` counts only `.java`. Documented in `license.py` docstring + PRODUCT_TIERS.md.
- **Threshold 500 Java files** — user-confirmed, generous, adoption-first.
- **Pro €19/mo · €190/yr per-dev** (down from €29.99) — focused CLI, sit at CodeScene floor / Cursor norm, not above. Per-LOC/per-repo reserved for a future org tier.
- **Free + Pro only, no Enterprise** — nothing org-level built (SSO/dashboards/hosted MCP); listed as roadmap.
- **MCP local Free, hosted = future Enterprise.**

## Required Reading (in order)

1. `docs/PRODUCT_TIERS.md` — full Free/Pro model + pricing rationale (source of truth)
2. `src/sourcecode/license.py` — `is_large_repo`, `require_repo_or_pro`, `_FREE_REPO_JAVA_FILE_LIMIT` (the gate)
3. `.planning/HANDOFF.json` — machine-readable state for /gsd-resume-work

## Critical Anti-Patterns (do NOT repeat)

- [ANTI-PATTERN]: "Fix" `is_large_repo` to count non-Java files → would start charging users we deliberately leave on Free. Java-only is intentional; see the docstring before touching it.

## Infrastructure State

- Both repos clean on `master`, all committed, unpushed.
- Gate is client-side (`cli.py`) + Apache 2.0 — no DRM. Accepted for v1; moat = convenience.

## Next Action

Start with: decide between (a) updating Lemon Squeezy pricing to €19/€190, or (b) pushing the three commits. Code work itself is done.
