# Continue Here — Session 75 handoff
_Generated: 2026-06-08_
_Version at pause: 1.35.22_
_Last commit: 133ce15_

---

## What Was Done This Session

### Full technical + product audit (principal engineer level)
Six-dimension audit: pillar validation, bug detection, architecture quality, product value gap analysis, prioritized roadmap, strategic verdict. Key outputs:
- Maturity map: TX/SEC audit = production-ready; javax→jakarta = beta; Java 8→17 = not implemented
- Critical bug B-3 identified: MCP server crash at import on registry drift
- Strategic verdict: `--ci` on migrate-check = highest 30-day revenue feature

### Phase 1 — "No crash, no silent fail, no hang" — COMPLETE (v1.35.22)

| Fix | File | Change |
|-----|------|--------|
| B-3: MCP server crash at import | `mcp/server.py:1320` | `validate_registry()` → try/except, RuntimeWarning, degraded mode |
| Silent pattern errors TX | `spring_tx_analyzer.py:675` | `except Exception: pass` → collect `_last_analysis_errors`, surface in `limitations[]` |
| Silent pattern errors SEC | `spring_security_audit.py:422` | Same pattern |
| MCP hang on get_spring_audit | `mcp/server.py:686` | ThreadPoolExecutor 2 min timeout (`SOURCECODE_SPRING_AUDIT_TIMEOUT_MS`) |
| MCP hang on get_impact_chain | `mcp/server.py:782` | ThreadPoolExecutor 1 min timeout (`SOURCECODE_IMPACT_TIMEOUT_MS`) |
| MCP hang on get_impact_context | `mcp/server.py:1199` | Same 1 min timeout |
| 5-module Maven cap silent | `dependency_analyzer.py:1155` | `MAX_SUBMODULES_REACHED: scanned 5 of N` in limitations |
| 8000-file scan cap silent | `repository_ir.py:3315` | `find_java_files(limitations=...)` optional param; `MAX_JAVA_FILES_REACHED` emitted |
| cli.py file cap propagation | `cli.py:3915, 4091` | spring-audit + migrate-check pass limitations to find_java_files, inject into output |

**Tests:** 2208 passed, 3 skipped (pre-existing Pro-gated)

---

## Next: Phase 2 — "Make it usable in CI"

### 2a. `--ci` flag on `migrate-check` — START HERE (highest ROI)
- File: `cli.py:4009` (`migrate_check_cmd`)
- Mirror `spring_audit_cmd` pattern: `--ci` bool option, exit code 1 when `blocking_count > 0`
- `MigrationReport` already has `blocking_count` field — just need CLI flag + exit logic
- `get_migration_readiness` MCP tool already exists — no MCP changes needed
- Add `--format github-comment` option (currently only `json | text`)
- Target CI usage:
  ```yaml
  - run: sourcecode migrate-check . --ci --format github-comment >> $GITHUB_STEP_SUMMARY
  ```

### 2b. Consistent structured output
- `SpringAuditResult.to_dict()` missing top-level `blocking_count` field
- `MigrationReport` already has it — spring-audit should match
- File: `spring_findings.py` — `SpringAuditResult.finalize()` / `to_dict()`

### 2c. GitHub Actions annotation format for migrate-check
- Spring-audit has `_render_spring_audit_github_comment()` at cli.py ~3200
- Need `_render_migrate_check_github_comment()` for MIG findings

### 2d. Visible limitations in migrate-check text format
- `MigrationReport.to_text()` doesn't show `limitations[]`
- Add limitations section to text output

---

## Remaining Bugs (NOT yet fixed)

| ID | Location | Description | Priority |
|----|---------|------------|---------|
| B-1 | `migrate_check.py:425` | Regex `.` instead of `[-.]` in SB2 version detection | Low |
| B-2 | `spring_security_audit.py:129` | SEC-001 false positive — no `@PermitAll` class-level check | Medium |
| B-6 | `canonical_ir.py:248-252` | `source_scope="method"` incorrectly set for class-level annotations | Medium |

---

## Long-deferred task (P3 from session 36)

E2E test + publish to PyPI/Homebrew:
- Run `sync_to_pipx.sh` from normal Terminal (not Claude Code)
- E2E verify 1.35.x fixes
- Publish to PyPI + Homebrew tap

---

## Known Flaky Tests (pre-existing, always exclude)

```
--ignore=tests/test_output_ux.py              # Pro delta quota exhausted in full suite
--ignore=tests/test_task_differentiation.py   # exit 2, Pro license required
--ignore=tests/test_signal_hierarchy.py       # exit 2, Pro license required
```

---

## Resume

```bash
python3 -m pytest tests/ -x -q \
  --ignore=tests/test_output_ux.py \
  --ignore=tests/test_task_differentiation.py \
  --ignore=tests/test_signal_hierarchy.py
```

**Start with:** `--ci` flag on `migrate-check` in `cli.py:4009`
