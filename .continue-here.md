# Continue Here — Session 86 Pause
**Timestamp:** 2026-06-10T18:30:00Z  
**Mode:** Búscar y Destruir (adversarial QA)  
**Commit:** 448f71c

---

## What Was Done This Session

### BUG-1 FIXED: test isolation — `lic.is_pro` state pollution
- **File:** `tests/test_license_ttl.py`
- **Cause:** `TestMaybeRevalidateCISkip` used bare `lic.is_pro = False` as teardown. On machines with pro license, original is `True` — teardown left module state wrong.
- **Symptom:** `test_delta_no_reasoning_key` failed in full suite (exit_code=2), passed in isolation.
- **Fix:** Replaced all `lic.is_pro = X` / `lic._license_data = X` bare assignments with `monkeypatch.setattr()` (auto-restores).

### BUG-2 FIXED: `compute_blast_radius` — `imports` edges = false-positive callers
- **Files:** `src/sourcecode/repository_ir.py`, `src/sourcecode/spring_impact.py`
- **Cause:** `_all_callers_from_rg` skipped `contained_in` but NOT `imports`. Any class with `import PatientService;` appeared as direct caller even with zero runtime dependency.
- **Example:** `impact PatientService openmrs-core` returned `ObsServiceImpl` as direct caller — only connection was an import statement.
- **Fix:** Added `_BLAST_SKIP_EDGE_TYPES = {"contained_in", "imports"}` in `repository_ir.py`. Now consistent with `spring_impact._SKIP_EDGE_TYPES`.
- **Also:** `spring_impact.py` CH-001b now only expands ORIGINAL user-query seeds (not seeds added by CH-001a), preventing sibling-implementor false positives via shared utility interfaces (e.g. `RefByUuid`).
- **Test fix:** Hub-class fixtures updated to use `@Autowired` so callers detected via `injects` edges (correct) not `imports` (now filtered).

**Result:** 2486 passed, 3 skipped, 0 failed.

---

## Remaining Attack Surface (Not Yet Tested)

Priority order:

### 1. `endpoints` — HIGH PRIORITY
- Inherited `@RequestMapping` (base class + subclass)
- Interface-defined mappings implemented by abstract/concrete class
- Compound path: class-level + method-level `@GetMapping`
- Test repo: `openmrs-core`, `keycloak`, `BroadleafCommerce`

### 2. `spring-audit` — HIGH PRIORITY
- Meta-annotations: custom `@Service` that is itself annotated with Spring annotations
- `@Transactional` on interface method vs impl method — does it detect both?
- `@Transactional(readOnly=true)` mix with write operations
- Test repo: `openmrs-core`, `spring-petclinic`

### 3. `chunk-file` — MEDIUM
- Methods > 200 lines
- Classes with generic type bounds (e.g. `<T extends Comparable<? super T>>`)
- Unicode identifiers
- Nested classes / anonymous inner classes

### 4. `impact-chain` — verify against `impact` fix
- Now that `imports` are filtered, does `impact-chain` agree with `impact`?
- Run both on same target, compare direct_callers

### 5. `rename-class` — edge cases
- Class name appears in string literals → should NOT rename
- Class name in comments → behavior?
- Partial name match (e.g. rename `User` when `UserService` exists)

### 6. `repo-ir --gzip` (P9 from session 85)
- Already committed but no adversarial test yet
- Test: `--gzip` without `--output` → should error
- Test: large repo → warning threshold fires

### 7. P3 (Windows cache) — BLOCKED
- `_atomic_write` in `cache.py:810` needs Windows env
- Static analysis only possible here

---

## How to Resume

```bash
cd /Users/user/Documents/workspace/atlas-cli
cat .continue-here.md
# Start next attack: endpoints inherited mappings
sourcecode endpoints /Users/user/Documents/workspace/testing/openmrs-core 2>/dev/null | python3 -c "import sys,json; d=json.load(sys.stdin); print('endpoints:', len(d.get('endpoints', d.get('route_surface',[]))))"
```

**Attack priority:** endpoints inherited mappings → spring-audit meta-annotations → chunk-file large files → full regression

---

## Test Repos Available
```
/Users/user/Documents/workspace/testing/
├── BroadleafCommerce   # large Spring, many endpoints
├── eureka              # Spring Cloud, minimal
├── jobrunr             # job scheduling
├── keycloak            # huge (7885 files), identity server
├── ofbiz-framework     # Java EE, non-Spring
├── openmrs-core        # Spring, medical, good impact tests
├── spaghetti-api       # small synthetic
└── spring-petclinic    # canonical Spring MVC
```
