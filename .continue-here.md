# Continue Here — Session 90 Pause

**Timestamp:** 2026-06-11T20:00:00Z  
**Version:** 1.35.33  
**Branch:** master (clean at `653a5f4`)  
**Tests:** 2499 passed, 3 skipped  

---

## What Was Done This Session

Session 90 fixed bugs from agent bug-fixing prompt (6 bugs, ordered by impact):

| Bug | Status | Commit | Root Cause |
|-----|--------|--------|-----------|
| BUG-1 cache never persists | ✅ Fixed | `afb0b11` | (1) `_core_key=""` for repos without git history — fixed with `nogit-{core_h}` key. (2) `core_view(sm)` in daemon thread exceeded 5s atexit join on 12k-file repos — fixed by precomputing in main thread |
| BUG-4 spring_boot_2_detected false + java.time.Calendar | ✅ Fixed | `9911c37` | Detection only checked root pom.xml; expanded to child modules (1 level deep) + gradle.properties + Gradle plugin pattern. MIG-016 hint `java.time.Calendar` (doesn't exist) → corrected to LocalDate/ZonedDateTime |
| BUG-5 has_uncommitted_changes false on untracked repo | ✅ Fixed | `f99b3f1` | `_has_uncommitted_changes` uses `--untracked-files=no` → misses all-untracked repos. Fix: promote to `True` when `_allowed_changed_files` non-empty |
| BUG-6 cache clear non-interactive | ✅ Already existed | — | `--yes` flag already on `cache clear` |
| BUG-2 custom security annotations | ⏳ Deferred | — | SAINT repo not on machine |
| BUG-3 @Autowired field injection callers | ⏳ Deferred | — | SAINT repo not on machine |

---

## Next Action

**BUG-2 and BUG-3 require the SAINT codebase** (`m3informatica.saint.*`).

`/Users/user/Documents/workspace/testing/` has open-source repos (keycloak, openmrs-core, eureka…) — NOT SAINT.

**To continue:** point agent to SAINT path or create minimal fixture.

### BUG-2: Custom security annotations
- `@M3FiltroSeguridad` (from `m3informatica.saint.rest.security`) not recognized → all endpoints `no_security_signal`
- Fix: add `security_annotations` config key to `.sourcecode.yml`, wire into endpoint security detection

### BUG-3: @Autowired field injection callers missing
- `impact-chain CalcularAtrasosTrieniosService` returns 0 callers despite `ConsolidacionTrienioRestService.java:34` `@Autowired` field injection
- Investigation trail:
  - `repository_ir.py:845` — field symbol gated on `_INJECT_ANNOTATIONS`
  - `repository_ir.py:1067` — `injects` edge creation
  - `repository_ir.py:1071` — uses `sym.imports_used`; fallback via `_same_pkg` at line 1075 for same-package types
  - `spring_impact.py:315` — BFS traversal of `injects` edges
- Likely root cause: same-package field injection — no import → `sym.imports_used` empty → `_same_pkg` fallback needs `sym.signature.split()[0]` to match; may be skipped if condition fails

---

## Files Changed This Session

```
src/sourcecode/cli.py           — BUG-1 (nogit key + core_view main-thread) + BUG-5
src/sourcecode/migrate_check.py — BUG-4 (_detect_spring_boot_2 + MIG-016 hint)
src/sourcecode/__init__.py      — version 1.35.32 → 1.35.33
pyproject.toml                  — version 1.35.32 → 1.35.33
```

---

## Environment

- Python: `/opt/homebrew/opt/python@3.14/bin/python3.14`
- Install: `python3.14 -m pip install -e . --break-system-packages`
- Testing repos: `/Users/user/Documents/workspace/testing/` (NOT SAINT)
- Cache now works: `sourcecode cache warm /path && sourcecode cache status /path` → Cores: 1, Views: 1

---

## Resume

```bash
cd /Users/user/Documents/workspace/atlas-cli
/opt/homebrew/opt/python@3.14/bin/python3.14 -m pip install -e . --break-system-packages
python3 -m pytest tests/ --tb=no -q
# Next: BUG-2 + BUG-3 (need SAINT repo path)
```
