# Continue Here — Session 76 Handoff
_Generated: 2026-06-08_
_Version at pause: 1.35.24_
_Last commit: ab732a3_

---

## What Was Done This Session

### Staff Engineer audit: migrate-check as enterprise migration tool

Full 7-phase evaluation of `migrate-check` against 8 real repos (BroadleafCommerce, Eureka,
Keycloak, OpenMRS, Spring PetClinic, OFBiz, JobRunr, Spaghetti API).

**Expanded from 8 rules → 20 rules** covering Java 8→17/21 + Spring Boot 2→3:

| Rule | Category | Severity | OpenRewrite? |
|------|----------|----------|--------------|
| MIG-009 | javax.jms → jakarta | medium | YES |
| MIG-010 | SecurityManager removed Java 17 | **critical** | NO |
| MIG-011 | sun.* / com.sun.net.* internal imports | high | NO |
| MIG-012 | Nashorn removed Java 15 | high | NO |
| MIG-013 | sun.misc.Unsafe | high | NO |
| MIG-014 | setAccessible(true) deep reflection | medium | NO |
| MIG-015 | finalize() override deprecated | medium | NO |
| MIG-016 | java.util.Date/Calendar/SimpleDateFormat | low | YES |
| MIG-019 | SpringFox incompatible Spring Boot 3 | high | NO |
| MIG-020 | antMatchers/authorizeRequests deprecated | high | YES |
| MIG-021 | JAXB removed from JDK Java 11 | high | NO |
| MIG-022 | JAX-WS removed from JDK Java 11 | high | NO |

**Report enhancements:**
- schema_version 1.0 → 1.1
- Each finding: `migration_target` + `openrewrite_recipe` fields
- Summary: `by_migration_target` breakdown
- Static `limitations[]` always emitted

**FP fixes:**
- MIG-010: Javadoc "SecurityManager" no longer matches (BroadleafCommerce: 5 → 1)
- MIG-012: ScriptEngineManager() constructor removed (OFBiz: 1 → 0)

**Tests:** 36 existing + 70 new = **106 total** in test_migrate_check.py, all pass.
11 new fixture Java files in tests/fixtures/javax_legacy/.
11/20 rules auto-fixable via official OpenRewrite recipes.

### Repo audit baseline (new rules applied)
| Repo | Score | Blockers | Key findings |
|------|-------|----------|--------------|
| BroadleafCommerce | 0/100 | 1 | MIG-010×1, MIG-014×25, MIG-016×144 |
| Eureka | 0/100 | 8 | javax.servlet/inject/ws.rs, sun.* |
| Keycloak | 0/100 | 20 | MIG-010×6, MIG-011×14, MIG-014×10 |
| OpenMRS | 0/100 | 1 | MIG-010×1, MIG-016×177 |
| Spring PetClinic | 100/100 | 0 | Already migrated |
| OFBiz | 0/100 | 23 | MIG-004×23, MIG-009×5 |
| JobRunr | 0/100 | 16 | MIG-011×16 (com.sun.net.httpserver) |

---

## Next (priority order)

### 1. `--target` filter flag — START HERE
`sourcecode migrate-check . --target java17|java21|springboot3`
- File: `cli.py` — `migrate_check_cmd` function
- Filter `_ALL_RULES` by `rule.migration_target` before scanning
- Users focused on SB3 migration don't want Java 9+ encapsulation noise

### 2. `--ci` flag on migrate-check (from session 75 plan)
- File: `cli.py` — `migrate_check_cmd`
- Exit code 1 when `blocking_count > 0`
- Add `--format github-comment` for CI annotation output

### 3. XML config scanning (biggest remaining gap)
- `applicationContext.xml`, `web.xml`, Spring XML bean definitions
- javax.* class names in XML beans not detected at all

### 4. pom.xml / build.gradle dependency version audit
- Detect incompatible library versions (Hibernate 5→6, SpringFox present, old Tomcat)

### 5. Remaining bugs from session 75 (NOT yet fixed)
| ID | Location | Description |
|----|---------|------------|
| B-2 | `spring_security_audit.py:129` | SEC-001 FP — no `@PermitAll` class-level check |
| B-6 | `canonical_ir.py:248-252` | `source_scope="method"` wrong for class-level annotations |

### 6. Regression tests for session-64 bugs (long pending)
- spring_tx_analyzer.py BUG-001/002
- repository_ir.py BUG-003
- spring_impact.py direct_callers dedup

### 7. GSD Phase 14 (Flujo de Ejecucion) — first unplanned GSD phase

---

## Known Flaky Tests (pre-existing, always exclude)

```bash
pytest tests/ -x -q \
  --ignore=tests/test_output_ux.py \
  --ignore=tests/test_task_differentiation.py \
  --ignore=tests/test_signal_hierarchy.py
```

---

## Resume

```bash
source .venv/bin/activate
sourcecode --version  # 1.35.24
pytest tests/test_migrate_check.py -q  # 106 pass
```

**Start with:** `--target` filter flag in `cli.py` migrate_check_cmd
