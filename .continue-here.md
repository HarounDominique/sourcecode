---
session: 64
date: 2026-06-04
status: paused
type: qa-audit (adversarial round 2)
---

# Continue Here — Session 64

## What was done

Second adversarial QA audit of Spring Semantic Engine — 4 real repos, 6 cycles, 3 new bugs found and fixed.

### Bugs fixed this session (commit `5d8a58b`)

| Bug | File | Root cause | Fix |
|---|---|---|---|
| BUG-001 | `spring_tx_analyzer.py` | TX-005 FP: `_has_swallowed_exception` used first-overload-wins + matched call-sites. 4 false positives (Broadleaf + OpenMRS). | Anchor preamble to last `}` for @Transactional check; add `;` before `{` guard to skip call-sites. |
| BUG-002 | `repository_ir.py` | Event publisher FN: two-step publish pattern `new Event(); publishEvent(var)` not detected — regex only matched inline `publishEvent(new Event(...))`. | Add `_PUBLISH_EVENT_CALL_RE` + `_NEW_EVENT_INSTANTIATION_RE`; emit `confidence=low` edge when both present. |
| BUG-003 | `spring_impact.py` | BFS hub-guard: raw sum of per-seed caller counts triggered `depth=1` cap prematurely on interface-expanded queries (36 methods × callers > 500). | Count unique callers (set) instead of raw sum. |

### Regression tests needed (NOT YET WRITTEN)

Tests for BUG-001, BUG-002, BUG-003 fixes not yet added. Must write before next version bump.

### Version: still v1.35.5 (no bump — regression tests not done yet)

## Open structural issue (not fixed — Phase 14 scope)

**BUG-004** (`spring_impact.py`): BFS dead-end on class-level FQN intermediates.
- `reverse_graph.get("OrderServiceImpl")` returns `{}` — graph keyed by method-level FQNs.
- `indirect_callers` stays 0 for interface queries even after BUG-003.
- Fix: build secondary class→method index in BFS traversal.

## Audit results summary

| Repo | Spring? | TX findings | FP/FN |
|------|---------|-------------|-------|
| BroadleafCommerce | Yes | 5 TX-005 (all TP) | 0 FP post-fix |
| OpenMRS Core | Yes | 10 TX-005 (all TP) | 0 FP post-fix |
| Keycloak | No (Quarkus) | 0 | correct |
| Confluent microservices-orders | Minimal | 0 | correct |

Determinism: 100% — 6 consecutive identical hashes.
Production readiness: **Internal Beta** (BUG-004 blocks Public Beta).

## Current GSD position

- Phase 13 (inferencia-arquitectonica): COMPLETE
- Phase 14 (Flujo de Ejecucion): NOT YET PLANNED
- All 13 phases complete, 44/44 plans done

## Immediate next actions (in order)

1. **Write regression tests for BUG-001/002/003** — then bump to v1.35.6
2. **Plan Phase 14** — `/gsd:discuss-phase` or `/gsd:plan-phase`
   - Scope: `--execution-flow` flag, request flow tracing, async event flows, critical path

## Backlog from session 62 (still pending)

1. MCP documentation — 26 tools, 0 documented
2. docs/schema.md — needs spring-audit/impact-chain output schemas
3. CHANGELOG.md — doesn't exist
