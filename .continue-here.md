# Continue Here — Buscar y Destruir (Session 92)
**Paused:** 2026-06-12
**Mission:** Systematic adversarial QA — find bugs, fix, regress. Loop until no criticals/highs open.
**Status:** 1 bug found and fixed. Loop continues. No criticals/highs open.

---

## Position

Active mission: `Buscar y Destruir — Estabilización integral de sourcecode`
Session 92 complete. One fix committed (`911d70e`). Tests green.

---

## Completed This Session

### Fix applied
| ID | Commit | Summary |
|----|--------|---------|
| ISSUE-001 | `911d70e` | `impact ""` — empty string matched all symbols, exits 0 → now exits 1 with INVALID_INPUT error |

### Adversarial fixtures created in testing/spaghetti-api:
- `TxService.java` — TX-001 violation (private @Transactional) — verifies `spring-audit --ci` exit 1
- `BrokenSyntax.java` — unclosed brace — verifies safe degradation with malformed Java
- `UnicodeService.java` — UTF-8 javadoc/strings — verifies encoding handling

### Attack categories COVERED:
- ✅ Invalid inputs: empty string, nonexistent path, bad format/scope
- ✅ Exit code consistency (re-tested without pipe interference — all correct except ISSUE-001)
- ✅ Edge cases: empty repo, no-git dir, path with spaces, unicode content
- ✅ Malformed Java files
- ✅ Large repo performance: keycloak 7885 files (spring-audit 3.4s, endpoints 4.2s, migrate-check 6.1s)
- ✅ `spring-audit --ci` gate: exit 1 with findings, exit 0 clean, --min-severity filtering
- ✅ `spring-audit --format github-comment`: verified
- ✅ Cache commands: status/freshness/clear behavior
- ✅ `chunk-file`: out-of-range chunk, nonexistent file, --metadata-only
- ✅ `rename-class`: empty name, nonexistent class
- ✅ `explain`: format json, nonexistent class
- ✅ `prepare-context`: invalid task, invalid git ref
- ✅ `pr-impact`: valid input, nonexistent files list
- ✅ `cold-start`: cached repo, no-cache repo

### Attack categories REMAINING:
- ⏳ `openmrs-core` and `ofbiz-framework` — spring-audit not run yet
- ⏳ `repo-ir` edge cases: --max-nodes/--max-edges, --gzip, --since with invalid ref
- ⏳ `impact-chain` deep traversal edge cases
- ⏳ Concurrent/parallel invocations (two `sourcecode` processes same repo simultaneously)
- ⏳ Cache corruption: corrupt cache file, truncated cache, wrong schema version
- ⏳ BroadleafCommerce deep dive (2985 files) — only spring-audit/endpoints run so far
- ⏳ `modernize` on keycloak (large repo)
- ⏳ `onboard` on monorepo (jobrunr or BroadleafCommerce)
- ⏳ `migrate-check` on openmrs-core and spring-petclinic

---

## Pre-existing Open Item

**BUG-006** — BroadleafCommerce `endpoints`=213 vs `spring-audit endpoints_analyzed`=228
- Pre-existing, not a regression from this session
- Neither side's extra files have REST annotations — pipeline counting difference
- If continued: trace `_build_route_surface` call differences between `extract_java_endpoints` and `build_repo_ir → ir_dict_to_canonical`

---

## Baseline Timings
| Repo | Command | Time |
|------|---------|------|
| spring-petclinic (47 files) | --compact | 0.29s |
| keycloak (7885 files) | --compact | 0.29s |
| keycloak | spring-audit | 3.4s |
| keycloak | endpoints | 4.2s |
| keycloak | migrate-check | 6.1s |
| BroadleafCommerce (2985 files) | spring-audit | 2.1s |

---

## Test Count
**2511 passed, 3 skipped** (unchanged from session 91)

---

## Global Constraints (active)
- Test repos at `/Users/user/Documents/workspace/testing/` — editable, use as lab
- One fix per commit
- Baseline at `/tmp/baseline/` (rebuilt at session start — /tmp is ephemeral)
- Issues in `/tmp/issues/ISSUE-NNN.md`
- Closure report at `/tmp/informe_estabilizacion.md`

---

## To Resume

```bash
git log --oneline -5            # verify last commit 911d70e
python3 -m pytest tests/ -q     # verify 2511 passed, 3 skipped
# Then continue attack loop — next targets:
# 1. openmrs-core spring-audit
# 2. repo-ir edge cases (--max-nodes, --gzip, --since invalid)
# 3. concurrent invocations
# 4. cache corruption
```

**Next attack command:**
```bash
mkdir -p /tmp/baseline/openmrs-core /tmp/issues
cd /Users/user/Documents/workspace/testing/openmrs-core
time sourcecode spring-audit . > /tmp/baseline/openmrs-core/spring-audit.json 2>&1
```
