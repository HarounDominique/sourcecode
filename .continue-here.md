---
session: 63
date: 2026-06-04
status: paused
type: qa-audit + regression tests
---

# Continue Here — Session 63

## What was done

Full adversarial QA audit of the Spring Semantic Engine across 4 real repos:
- BroadleafCommerce, Keycloak, Confluent Kafka examples, OpenMRS core
- All repos at `/Users/user/Documents/workspace/testing/`

### Bugs fixed (commit `92cc793` — v1.35.4)

| Bug | File | Root cause | Fix |
|---|---|---|---|
| BUG-PARSER-001 | `repository_ir.py` | `_CLASS_DECL_RE` requires `{` same line as `class` — multi-line decls produce 0 symbols | Pre-join continuation lines until `{` found |
| BUG-IC-001 | `cir_graphs.py` | `implements` edges store simple names; `known_symbols` filter drops all of them | `_simple_to_fqn` precomputed lookup for unambiguous resolution |
| BUG-IC-002 | `spring_impact.py` | Callers inject interface not impl — BFS from impl seeds finds nothing | CH-001b: expand impl seeds to include their interfaces |
| BUG-IC-003 | `spring_impact.py` | Field injection FQNs use `.` format; CH-002 checked `#` only — BFS stopped at field node | Detect lowercase last segment after `.` as field injection |
| BUG-EVT-001 | `repository_ir.py` | `\bApplicationListener` word-boundary breaks on `BroadleafApplicationListener` prefix | Changed to `\w*ApplicationListener` |

### Regression tests added (commit `48b1651` — v1.35.5)

12 new tests across 4 files:
- `tests/test_cir_graphs.py` — IG-09/10: simple-name resolution (BUG-IC-001)
- `tests/test_spring_impact.py` — CH-001b: impl→interface BFS expansion (BUG-IC-002)
- `tests/test_repository_ir.py` — PARSER-001: multi-line class declarations
- `tests/test_spring_event_topology.py` — EVT-APP-6/7: BroadleafApplicationListener<T>

### Version bump

v1.35.4 → **v1.35.5** — pyproject.toml, `__init__.py`, README.md

## Final test state

**2117 passed, 3 skipped — all green**

## Structural limitation documented (not fixed)

**LIMIT-EVT-001** — Variable-pattern event publishing not detected:
`var = new Event(); publishEvent(var)` — requires dataflow analysis.
Marked `confidence: medium` in output. No fix attempted.

## Verified against real repos

All 5 bugs confirmed fixed against real repos (not just unit tests):
- TransactionLifecycleMonitor: 47 symbols extracted (was 0)
- OrderServiceImpl: 72 direct callers (was 0)
- TransactionLifecycleEvent: 1 publisher, 1 consumer (was 0 consumer)
- ImplementationGraph: 894 impl relationships (was 0 for simple-name edges)
- Determinism: 100% — identical output run-to-run

## What's still missing (next session)

From session 62 handoff (unchanged):
1. MCP documentation — no docs for get_impact_chain, get_spring_audit, start_session, flow runner MCP tools (26 tools total, 0 documented)
2. docs/schema.md — not updated with spring-audit/impact-chain output schemas
3. Landing quickstart demo — still shows old impact terminal demo; needs spring-audit/impact-chain output
4. CHANGELOG.md — doesn't exist; worth creating for v1.35.x changes
5. GSD Phase 14 — still 0 plans in ROADMAP despite features being implemented (sessions 37-61)

## Next action

Pick up from session 62 next-action: Create `docs/MCP.md` covering all 26 MCP tools with parameters, examples, and agent workflows.
