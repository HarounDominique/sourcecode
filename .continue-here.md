# Continue Here — Session 78 Handoff
_Generated: 2026-06-09_
_Version at pause: 1.35.25_
_Last commit: 2ac35f8_

---

## What Was Done This Session

### Exhaustive 8-phase adversarial evaluation of Sourcecode CLI

Full simulation of enterprise engineering team (architect, migration expert, SRE, QA, security reviewer, agent builder). Tested against:
- Synthetic ACME Legacy project (Java 8 / Spring Boot 2.7, 3-module Maven, Hibernate 5, SpringFox, EhCache 2, XML Spring Security, web.xml)
- Real repos: spring-petclinic, BroadleafCommerce, openmrs-core, keycloak, eureka

**10 bugs found, 10 opportunities ranked, documentation gaps identified.**

---

## Bugs Found (evidence-backed)

| ID | Severity | Description | File/Location |
|----|----------|-------------|---------------|
| F-001 | **HIGH (P0)** | `endpoints` @PreAuthorize expression truncated — `"hasRole('USER'"` missing `)` | regex in endpoint extractor |
| F-002 | **HIGH (P1)** | MIG-041 false negative — Hibernate 5.x via `${hibernate.version}` property not detected | dep rule version matcher |
| F-003 | **MEDIUM (P1)** | Multi-module pom.xml inflates counts: EhCache×3, SpringFox×2 → effort_days 5.2d vs real ~3.5d | dep deduplication |
| F-004 | MEDIUM (P2) | `explain` outgoing_deps misses PaymentService (injected via constructor, called at runtime) | symbol resolution |
| F-005 | LOW (P3) | `impact` direct_callers includes `OrderController#<init>` noise | caller filter |
| F-006 | LOW (P3) | `onboard/explain` entity description says "no persistence annotation detected" despite `@Entity` | explanation template |
| F-007 | MEDIUM (P2) | `cold-start --compact` returns 78 tokens on small repo vs advertised "~10K tokens" | cold-start content floor |
| F-008 | HIGH (info) | BroadleafCommerce endpoints 98/110 `no_security_signal` — XML security opaque, no warning in output | endpoints XML security |
| F-009 | MEDIUM (P2) | Sync `@EventListener` inside TX not flagged as risk by spring-audit or impact-chain --type events | TX risk detection |
| F-010 | LOW (P3) | MCP status shows external server warning with no automated fix path | mcp status UX |

---

## Next (priority order)

### 1. Fix @PreAuthorize expression regex (F-001) — START HERE (P0)
**Confirmed bug**: `sourcecode endpoints /tmp/acme-legacy` → `"expression": "hasRole('USER'"` (missing `)`)
- Location: endpoint extractor regex in `src/sourcecode/` (find the `@PreAuthorize` parsing code)
- Fix: The regex stops before closing `)` — extend pattern to capture full expression including balanced parens
- Test: `endpoints` on acme-legacy should show `"hasRole('USER')"` with closing paren

### 2. `--target` filter flag on migrate-check (P0 — from session 76 still pending)
- `sourcecode migrate-check . --target java17|java21|springboot3`
- Filter `_ALL_RULES + _XML_RULES + _DEP_RULES` by `rule.migration_target`
- File: `cli.py` — `migrate_check_cmd` function + `run_migrate_check()`
- Users doing SB3-only migration don't want Java 9+ encapsulation noise

### 3. Maven property resolution in MIG-041 (F-002) — High value (P1)
- Root pom: `<hibernate.version>5.6.15.Final</hibernate.version>` → `${hibernate.version}` in dependencyManagement
- MIG-041 misses this because it only checks literal version strings in dep declarations
- Fix: Parse `<properties>` block, substitute `${prop}` before version matching in dep rules
- File: `migrate_check.py` or `_scan_dep_file()` — wherever dep version extraction happens

### 4. Multi-module pom deduplication (F-003) — High value (P1)
- EhCache flagged 3× (root + api + data pom), SpringFox 2× → inflated effort estimate
- Fix: After scanning all poms, deduplicate findings by `(groupId, artifactId)` — keep only highest-severity instance
- Keep all occurrences in `findings[]` but mark duplicates, only count unique deps in effort calc

### 5. `--ci` flag on migrate-check (P1 — from session 77)
- Exit code 1 when `blocking_count > 0`
- Same pattern as spring-audit `--ci`
- File: `cli.py` — `migrate_check_cmd`

---

## Key Findings from Evaluation

### What works well (no action needed)
- Performance: BroadleafCommerce 2.88s cold, 0.198s warm — matches README claims exactly
- Zero false positives on spring-petclinic (Spring Boot 4) — 100/100 clean
- spring-audit SEC-003 correctly finds TX-in-controller pattern
- impact-chain --type events correctly traces publisher → AFTER_COMMIT + sync consumers
- pr-impact shows full event flow including both consumers
- XML scanning (MIG-030/031/032) works correctly
- spring-audit --ci respects --min-severity threshold correctly

### What needs documentation updates
1. migrate-check --help lists only MIG-001..008 → tool has 43 rules (OUTDATED)
2. cold-start --compact "~10K tokens" claim misleading for small repos
3. Enterprise migration workflow pattern missing from README
4. `prepare-context --fast` and `--task-help` undocumented
5. `migrate-check --ci` not mentioned in main README

---

## Competitive Position (from evaluation)

**Unique/best-in-class:**
- Spring TX boundary + event topology + security surface in one unified call graph
- XML Spring migration detection (no competitor has this)
- Agent-optimized deterministic JSON output

**Gaps vs competitors:**
- No automated fix (OpenRewrite has this; major gap)
- XML security opaque (BroadleafCommerce pattern)
- Java/Spring only (SonarQube, CodeQL are polyglot)

**Verdict determined:** **STRONG** (not yet Enterprise Ready due to F-002/F-003)

---

## Known Flaky Tests (pre-existing, always exclude)

```bash
pytest tests/ -x -q \
  --ignore=tests/test_output_ux.py \
  --ignore=tests/test_task_differentiation.py \
  --ignore=tests/test_signal_hierarchy.py
```

---

## Resume

```bash
source .venv/bin/activate
sourcecode --version  # 1.35.25
pytest tests/test_migrate_check.py -q  # 166 pass
```

**Start with:** Fix @PreAuthorize expression regex in endpoint extractor (F-001).
Find the regex with: `grep -rn "PreAuthorize\|hasRole\|preauthorize" src/sourcecode/ --include="*.py" | grep -i "regex\|pattern\|re\."` 
