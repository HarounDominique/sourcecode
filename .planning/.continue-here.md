---
context: default
phase: none (post-milestone field-audit work)
task: 5
total_tasks: 5
status: paused
last_updated: 2026-06-16T18:49:39Z
---

<current_state>
Post-milestone, field-audit-driven work. No active GSD phase. Two releases shipped
this session and committed to master (not pushed):

- v1.45.0 (b6b96ab) — 4 SAINT audit fixes: P1 cache-clear hang, #2 --no-cache,
  #3 endpoints --limit counters, #5 validation no-OpenAPI note.
- v1.46.0 (3997665) — Java/Spring MCP orchestrator flow presets + intent routing.

All work committed. Tree clean except this handoff. Tests: 2669 passed, 3 skipped.
HEAD is 2 commits ahead of origin/master — NOT pushed.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: P1 cache-clear hang (non-interactive stdin) — Done (b6b96ab)
- Task 2: --no-cache accepted on 6 analysis subcommands — Done (b6b96ab)
- Task 3: endpoints --limit counter coherence — Done (b6b96ab)
- Task 4: validation no-OpenAPI explicit note — Done (b6b96ab)
- Task 5: orchestrator flow presets (run_migrate_flow, run_security_audit_flow) +
  INTENT_MIGRATION/INTENT_SECURITY_AUDIT + R5/R6 rules — Done (3997665)
</completed_work>

<remaining_work>

- Task 6: Audit bug #4 — endpoints config-less none_detected heuristic/auto-detect.
  NOT started. Needs SAINT repo or synthetic fixture.
- Task 7: Audit bug #6 — class-level security annotation propagation to handlers
  (SAINT: 1136 custom vs 2549 real occurrences). NOT started. Meatier semantic fix.
  Needs SAINT repo to reproduce.
</remaining_work>

<decisions_made>

- Release commits go directly to master (matches repo convention; solo repo, no PR flow).
- Flow presets add NO new analysis — orchestration ergonomics only. Genuine value =
  config-less security safeguard (prevents false negative). Documented honestly in
  CHANGELOG (no-trap-promo rule).
- P3 explain exit-code item closed as already-fixed (cli.py:4781 raises Exit(1);
  audit ran v1.42, fixed by v1.44).
</decisions_made>

<blockers>
- Audit bugs #4 and #6: not reproducible on local testing repos (petclinic/Broadleaf).
  Both are custom-annotation (@M3FiltroSeguridad-style) security detection. Workaround:
  build a synthetic Java fixture with a class-level custom security annotation +
  multiple handlers and no method-level annotation.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable state, full task/commit detail.
2. `CHANGELOG.md` [1.45.0] + [1.46.0] — what shipped and the honest framing.
3. Memory `finding_ch004_crossmodule_interface_endpoints.md` + `feedback_honest_benchmarks.md`
   — prior security-detection findings + the no-trap-promo rule.

## Critical Anti-Patterns (do NOT repeat these)
- [WEAK-REPRO]: Verifying the P1 hang with `< /dev/null` gave a false pass — /dev/null
  sends immediate EOF (click raises Abort), it does NOT reproduce the open-pipe hang.
  → Reproduce stdin-blocking bugs with a never-EOF pipe (`< <(sleep 60)`), not /dev/null.
- [SILENT-STRIP]: New @mcp.tool() functions are silently removed at import by
  `_finalize_mcp_registry()` unless added to `_NATIVE_MCP_TOOLS` in server.py.
  → When adding a native MCP tool, update _NATIVE_MCP_TOOLS AND the
  test_mcp_registry_consistency.py native set.

## Infrastructure State
- Python: use system /opt/homebrew/bin/python3 for tests (venv SIGKills, exit 137).
- Git: master, HEAD 3997665, 2 commits ahead of origin/master, NOT pushed.

<context>
Field-audit on repo SAINT (7488 files, 3996 endpoints) drove the whole session.
Every fix verified live on testing/spring-petclinic before commit. No known
locally-reproducible bugs remain. The two open items (#4/#6) are gated on the SAINT
repo or a synthetic custom-annotation fixture.
</context>

<next_action>
Start with one of:
(a) Build a synthetic Java fixture (class-level custom security annotation + multiple
    handlers, no method-level annotation) to reproduce + fix audit bugs #4/#6; or
(b) `git push` the two local releases (v1.45.0 + v1.46.0); or
(c) Begin unrelated next work.
</next_action>
