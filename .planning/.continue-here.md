---
context: default
phase: none (post-milestone benchmark + product strategy)
task: 4
total_tasks: 4
status: paused
last_updated: 2026-06-16T19:55:41Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| DI-BLINDSPOT | `impact`/`impact-chain` returned `0 callers` for `LocalRedirectStrategy` (interface + Spring DI wired). Static call-graph misses framework wiring → looks "unused" but isn't. | advisory | When a symbol shows 0 callers, cross-check wiring/config (grep beans, interface impls) before concluding isolation. See memory finding_benchmark_di_mediated_blindspot. |
| TRUE-NEGATIVE-CHECK | #2232 was already fixed in 7.0.x (named query `_WITH_LOCALE` already pushes the filter). Almost fixed a non-bug. | advisory | Before fixing any issue, read the CURRENT code on the target branch to confirm it's still unfixed. |
| VERIFY-WITHOUT-MAVEN | No `mvn`/`mvnw` in this env (only `gh`, `git`, `java 21`, populated `~/.m2`). | advisory | Verify Java fixes standalone: `javac -cp <jars from ~/.m2> -d out src test` then `java ... org.junit.runner.JUnitCore <Test>`. Worked for #3124 (6/6). |

<current_state>
Post-milestone benchmark + strategy session. NO active GSD phase. Goal was to prove
sourcecode's genuine value on a real maintained OSS repo and decide product direction.
Two PRs opened on BroadleafCommerce from fork HarounDominique; both OPEN. All artifacts
written. atlas-cli tree clean (.planning gitignored except this file + HANDOFF.json).
</current_state>

<completed_work>

Completed Tasks:
- Task 1: PR #3131 — Fix #3124 open-redirect in LocalRedirectStrategy — Done. SHOWCASE WEAK (tool didn't light up: DI blindspot). 6/6 JUnit verified standalone.
- Task 2: PR #3132 — Fix #2280 index on BLC_FIELD.ABBREVIATION — Done. SHOWCASE STRONG (impact-chain lit up: 4 direct/13 transitive, guided selection, ruled out #2232).
- Task 3: Benchmark report + honest scoring — Done (`.planning/benchmark-broadleaf-3124.md`).
- Task 4: Product/monetization strategy — Done (`.planning/ESTRATEGIA-PRODUCTO-TODO.md` + memory).
</completed_work>

<remaining_work>

- Task 5: Watch PR #3131 + #3132 maintainer review (#3131 contends with competing PR #3126). Not started.
- Task 6: CORE — fix DI/interface-mediated call-graph blindspot (highest-leverage, on-strategy). Not started. AC: reproduce with LocalRedirectStrategy; impact-chain should report login flows that wire it.
- Task 7: Reframe deferred post-milestone audit items #4/#6 (custom-annotation security) as ENRICHMENT, not detection, per new strategy. Not started.
</remaining_work>

<decisions_made>

- FOCUS the product on deterministic structural intel (impact-chain, migrate-check, pr-impact, MCP). Do NOT build security detection — benchmark proved it useless on DI-mediated security (#3124) and decisive on call-chain impact (#2280).
- Monetization: open-core + PLG via MCP; per-seat Pro on high-value verbs; per-project migration SKU; REPLACE the current class-count cap with command-tier + seat gating (never cap onboarding).
- Picked #2280 for the strong showcase (#2232 was already fixed; #2280 = hot path + safe verifiable fix).
- Left PR bodies generic (no sourcecode mention) — self-promo risk.
</decisions_made>

<blockers>
- None active. PR mergeability depends on Broadleaf maintainers; #3131 contends with competing PR #3126.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable full state.
2. `.planning/ESTRATEGIA-PRODUCTO-TODO.md` — the product direction + monetization decisions (the durable output of this session).
3. `.planning/benchmark-broadleaf-3124.md` — benchmark evidence + honest scoring.
4. Memories `project_product_strategy`, `finding_benchmark_di_mediated_blindspot`, `feedback_honest_benchmarks`.

## Infrastructure State
- Python: system `/opt/homebrew/bin/python3` (venv SIGKills, exit 137).
- `gh` CLI authenticated as HarounDominique (scope `repo`). No manual token in play.
- broadleaf-fieldtest: 2 branches pushed to fork; PRs #3131 + #3132 OPEN; local HEAD on `fix/2280-index-blc-field-abbreviation`. Upstream base `develop-7.0.x`.
- atlas-cli: clean, HEAD 211d697.

<context>
Benchmark-driven, not phase execution. The whole session was: prove sourcecode's honest
value on a real repo, then set product strategy. The big takeaway is captured in the
strategy doc — its single most important action item is the CORE DI/interface call-graph
fix (Task 6), because it turns the tool's biggest demonstrated weakness into core quality
without scope-creeping into security.
</context>

<next_action>
Start with one of: (a) check PR #3131/#3132 review status (`gh pr view <n> --repo BroadleafCommerce/BroadleafCommerce`); (b) open a GSD phase for the DI/interface call-graph blindspot fix (Task 6, on-strategy, high leverage); or (c) reframe audit items #4/#6 as enrichment.
</next_action>
