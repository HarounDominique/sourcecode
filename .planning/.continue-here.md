---
context: default
phase: none (ad-hoc product + landing work, post-milestone v1.53)
task: 6
total_tasks: 10
status: paused
last_updated: 2026-06-19T14:08:58+0200
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints discovered this session. Trees clean in both repos._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| .venv python SIGKILL (137) | Running tests/CLI with the project `.venv` python gets killed | advisory | Use system `/opt/homebrew/bin/python3` |
| `python -m sourcecode.cli` silent | Produces no output / fails silently | advisory | Invoke as `PYTHONPATH=src /opt/homebrew/bin/python3 run_cli.py <cmd>` |
| Headless preview blank | `.fade-in` opacity is gated by IntersectionObserver; isolated previews without the page JS render blank | advisory | When screenshotting isolated sections, inject `.fade-in{opacity:1!important;transform:none!important}` |
| IDE auto-commits landing | IntelliJ auto-commits `sourcecode-landing` ("Update index.html" / "actualizando…") between manual commits; git HEAD shifts unexpectedly | advisory | Verify `git merge-base --is-ancestor` + grep content markers before assuming work is lost |

<current_state>
No active GSD phase. This session was ad-hoc product + marketing work on two repos:
`atlas-cli` (the CLI) and `sourcecode-landing` (the marketing site). All work is
committed in both repos; both working trees are clean. Nothing pushed yet (user
pushes manually). The v1.53 milestone remains complete; this built v1.54→v1.57 on top.
</current_state>

<completed_work>

atlas-cli (CLI):
- v1.54.0 — C4 module-root rollup in `export --c4` (module_roots) — pushed
- v1.55.0 — security hardening (license-file 0600/0700, endpoint URL validation, grep -F) — `a97db33`, pushed
- v1.56.0 — CH-007 external-interface DI caller recovery (spring_impact.py) — `990142e`, pushed
- v1.57.0 — TEMPORARY Pro unlock: `_PRO_UNLOCK_ALL` floors `is_pro=True` (env `SOURCECODE_PRO_UNLOCK`, default on); gate logic untouched; 4 tests in `TestProUnlock` — `9560796`, NOT pushed. 2728 tests pass.

sourcecode-landing (marketing):
- Pricing reframed to "temporarily no payment / fully open" banner; version → 1.57.0; current commands added (migrate-check, export --c4); meta/og/hero refreshed — `94f7c76`, NOT pushed
- New `#output` showcase section: 3 terminal panels (migrate-check, spring-audit, export --c4) built from REAL Broadleaf runs (2,762 files); nav link + EN/ES i18n; reuses `.t-*` design system + new CSS (readiness score block, severity chips, finding rows) — `2973953`, NOT pushed. Verified by Chromium headless screenshots.
</completed_work>

<remaining_work>

- Task 7: PUSH both repos — atlas-cli `9560796`, sourcecode-landing `2973953` (user does this manually)
- Task 8: Strategy priority #3 — public reproducible migrate-check proof
- Task 9: Strategy priority #4 — multi-repo regression corpus
- Task 10 (optional): model setter/XML-injection edges so Broadleaf `RedirectStrategy` (plain field+setter, no `injects` edge) becomes recoverable by CH-007; transitive depth via the interface's methods
</remaining_work>

<decisions_made>

- Pro paywall DISABLED via a reversible flag (`_PRO_UNLOCK_ALL`), NOT by deleting the gate logic — resume later with a 1-line flip. Recorded in memory `project_pro_unlock_temporary.md`.
- Landing pricing presented as "temporarily open at no cost" (payment off), NOT as a Free tier — per explicit user instruction; the future paid model stays visible.
- Output showcase demos 3 NEW moat commands (not the hero's `impact`), all numbers from real Broadleaf runs — evidence over claims.
</decisions_made>

<blockers>
- None.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — structured machine-readable state (commits, push status, tasks)
2. memory `project_pro_unlock_temporary.md` — the temporary Pro-unlock mechanism and how to resume the paywall
3. `.planning/ESTRATEGIA-PRODUCTO-TODO.md` — product strategy / priorities #3–#4

## Infrastructure State
- atlas-cli: clean tree, HEAD `9560796` (unpushed). Tests: `/opt/homebrew/bin/python3 -m pytest` (2728 pass).
- sourcecode-landing: clean tree, HEAD `2973953` (unpushed). Static `index.html`; preview with Chromium headless.
- No running background services.

<context>
Everything is committed and verified; the only "open" item is the manual push the
user always does. The natural next thrust for adoption (the stated goal) is the
strategy backlog priorities #3 (public reproducible migrate-check proof) and #4
(regression corpus), or the optional CH-007 depth work. Caveman mode is active.
</context>

<next_action>
Start with: push both repos (atlas-cli `9560796`, sourcecode-landing `2973953`) if the
user wants them live; otherwise pick strategy priority #3 (public reproducible
migrate-check proof). Nothing is blocked.
</next_action>
