---
context: default
phase: null
task: null
total_tasks: 0
status: paused
last_updated: 2026-06-12T17:45:00Z
---

# Session 93 — Bug Fixes + Attack Loop Pass 2

_6 issues fixed this session. Tree clean at `8bbb616`._

<current_state>
Session 93 adversarial QA pass 2 (segunda pasada desde cero). Fixed BUG-06, BUG-07, and 5 input-validation gaps discovered in attack loop.
2514 tests pass, 3 skipped.
README.md has ~211 lines of uncommitted documentation (migrate-check, cache freshness, spring-audit --ci, repo-ir flags, explain, pr-impact).
</current_state>

<completed_work>

- **BUG-06** (low) — `project_type: unknown` on openmrs-core (multi-module Maven). Root: `JavaDetector` only read root `pom.xml`; Spring deps live in child module `web/pom.xml`. Fix: added `_get_child_pom_paths()` to scan `<modules>` children. Regression test added. Commit `9fa10db`.

- **BUG-07** (low) — Eureka classified as Jakarta EE. Root: `"javax.ws.rs" in text` matched `javax.ws.rs:jsr311-api` (JAX-RS 1.x API spec jar — just interfaces, not a server). Fix: replaced broad check with specific signals (`javax.ws.rs-api`, `jakarta.ws.rs`, `javaee-api`, `jakartaee-api`, `resteasy`). Regression tests added. Commit `9d466ea`.

- **Attack loop pass 2 — 5 validation gaps fixed:**

  | Issue | Command | Bug | Fix |
  |-------|---------|-----|-----|
  | A | `impact-chain ""` / `"   "` | exit 0, silent `not_found` | `symbol.strip()` guard + exit 1 |
  | B | `explain ""` / `"   "` | exit 0, markdown "Class not found" | `class_name.strip()` guard + exit 1 |
  | C | `pr-impact --files ""` | `IsADirectoryError` traceback | `files.is_dir()` guard added |
  | D | `endpoints --format xml` | silently ignored, returned JSON | format validation before analysis |
  | E | `chunk-file *.py` | processed as Java, garbage output | `.suffix != ".java"` guard |

  Commits `ceefbfe` + `8bbb616`.

- **Attack loop methodology note:** pipe (`| head -N`) poisons `$?` — exit codes from piped output are `head`'s, not the command's. Verified all real issues without pipes.
</completed_work>

<remaining_work>

- **README.md** — ~211 lines uncommitted. Documents: `migrate-check`, `cache freshness`, `spring-audit --ci`, `repo-ir` size-control flags (`--max-nodes`, `--max-edges`, `--gzip`, `--force`, `--include-tests`), `explain`, `pr-impact`. Needs review + commit.
- **P3** (session 85, blocked) — Windows cache slow (`cache.py:810` `_atomic_write`). Not reproducible on macOS.
- **Attack loop pass 3** (optional) — passes 1 and 2 complete. Could run pass 3 targeting: real Java repos with actual data, concurrent/MCP scenarios, output flag combinations.
</remaining_work>

<decisions_made>

- BUG-07: Chose NOT to add "Jersey" as a separate framework. Kept Jakarta EE label for Jersey-based REST servers — only tightened the signal. Rationale: adding a new framework label has classifier.py ripple effects; minimal fix preferred for low-severity bug.
- Attack loop: pipe artifact discovery (`| head -N` poisons `$?`) noted as methodology rule for all future loop runs.
- `endpoints --limit 0` / `--limit -1` silently treated as no-limit: accepted as-is (minor UX, not crash, not worth fixing now).
</decisions_made>

<blockers>

- P3 requires Windows to validate.
</blockers>

## Key Files Modified This Session

| File | Change |
|------|--------|
| `src/sourcecode/detectors/java.py` | `_get_child_pom_paths()` + child pom scanning in `detect()` (BUG-06); tightened Jakarta EE signal (BUG-07) |
| `tests/test_detector_go_rust_java.py` | BUG-06 regression test + 2 BUG-07 regression tests |
| `src/sourcecode/cli.py` | 5 validation fixes: impact-chain, explain, pr-impact, endpoints, chunk-file |

## Bug Queue Status

| ID | Sev | Status | Commit |
|----|-----|--------|--------|
| BUG-01 | medium | ✅ fixed | 06e9c26 |
| BUG-02 | high | ✅ fixed | 76d4beb |
| BUG-03 | high | ✅ fixed | 76d4beb |
| BUG-04 | medium | ✅ fixed | 15e3af7 |
| BUG-05 | medium | ✅ fixed | cf3d699 |
| BUG-06 | low | ✅ fixed | 9fa10db |
| BUG-07 | low | ✅ fixed | 9d466ea |
| P3 | — | blocked (Windows) | — |

## Infrastructure State

- Version: v1.35.33
- Tests: 2514 passed, 3 skipped
- Tree: clean (README.md has uncommitted doc changes)
- Branch: 4 commits ahead of origin/master (not pushed)

<next_action>
Option A: Commit README.md docs — review ~211 lines, stage, commit.
Option B: Attack loop pass 3 — target real Java repos with actual data flows, MCP scenarios.
Option C: Push branch to origin (4 commits pending).
Recommended: Option A (README docs) then push.
</next_action>
