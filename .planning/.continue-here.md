---
context: default
phase: null
task: null
total_tasks: 0
status: paused
last_updated: 2026-06-11T12:35:00Z
---

# Session 89 — Bug Fixes (BUG-01 through BUG-05 done)

_4 bugs fixed this session. Tree clean at `cf3d699`._

<current_state>
Session 89 bug-fix run. Processed BUG-01 through BUG-05 from session 88 evaluation queue.
BUG-06 and BUG-07 remain (both low severity). P3 (Windows cache) still open.
2499 tests pass, 3 skipped.
</current_state>

<completed_work>

- **BUG-01** (medium) — Spanish limitation string in `--agent` output. Fixed `architecture_analyzer.py`. Commit `06e9c26`.
- **BUG-02 + BUG-03** (high) — Risk scoring inconsistency (`impact` HIGH vs `impact-chain` CRITICAL for same score=25). Fixed shared threshold table in `risk_scoring.py`. Commit `76d4beb`.
- **BUG-04** (medium) — OFBiz Gradle parse: phantom `group:module` deps from block-comment regex match + real 87 deps missing because `apply from:` not followed. Fixed `dependency_analyzer.py` (`_strip_gradle_comments` + `apply from:` traversal). Commit `15e3af7`.
- **BUG-05** (medium) — `spring_detected: True` on Keycloak (Quarkus/JAX-RS). Root cause: 66 JPA `@Entity` classes were counted as Spring beans in `BeanGraph`. Added `has_spring_beans()` to `BeanGraph` (excludes JPA-only stereotypes). Also removed `security_model != "unknown"` false proxy from security auditor. Commit `cf3d699`.
</completed_work>

<remaining_work>

- **BUG-06** (low) — `project_type: unknown` on openmrs-core despite 30 TX boundaries + Spring MVC. Investigate `repo_classifier.py` or wherever `project_type` is set. openmrs-core is at `/Users/user/Documents/workspace/testing/openmrs-core`.
- **BUG-07** (low) — Eureka framework identified as Jakarta EE (uses Guice/Jersey, not Jakarta EE). Same classifier location.
- **P3** (session 85, blocked) — Windows cache slow (`cache.py:810` `_atomic_write`). Not reproducible on macOS.
</remaining_work>

<decisions_made>

- BUG-05 fix only addresses `spring_tx_analyzer` and `spring_security_audit` paths. The `ris.py` `_spring_detected` (based on REST layer detection) is a separate concern and was intentionally NOT changed this session — it's used for validation (incomplete_snapshot detection), not just repo_type labeling. Revisit if orchestrator still mislabels after spring-audit.
- `@Consumes` → "controller" mapping in `_JAVA_ROLE_MAP` (repository_ir.py:286) is a separate issue from BUG-05; not fixed because Keycloak structural_map showed 0 controllers regardless.
</decisions_made>

<blockers>

- P3 requires Windows to validate.
</blockers>

## Key Files Modified This Session

| File | Change |
|------|--------|
| `src/sourcecode/spring_model.py` | Added `_JPA_ONLY_STEREOTYPES` + `BeanGraph.has_spring_beans()` |
| `src/sourcecode/spring_tx_analyzer.py` | Use `has_spring_beans()` instead of `bool(bean_graph.beans)` |
| `src/sourcecode/spring_security_audit.py` | Same + removed `security_model != "unknown"` proxy + import `BeanGraph` |
| `src/sourcecode/dependency_analyzer.py` | `_strip_gradle_comments()` + `apply from:` traversal in `_analyze_gradle()` |
| `tests/test_spring_security_audit.py` | Updated `test_spring_detected_true` + regression test for BUG-05 |
| `tests/test_spring_tx_analyzer.py` | Regression tests: entity-node must not trigger spring_detected |
| `tests/test_dependency_analyzer_polyglot.py` | OFBiz regression test (BUG-04) |

## Bug Queue Status

| ID | Sev | Status | Commit |
|----|-----|--------|--------|
| BUG-01 | medium | ✅ fixed | 06e9c26 |
| BUG-02 | high | ✅ fixed | 76d4beb |
| BUG-03 | high | ✅ fixed | 76d4beb |
| BUG-04 | medium | ✅ fixed | 15e3af7 |
| BUG-05 | medium | ✅ fixed | cf3d699 |
| BUG-06 | low | pending | — |
| BUG-07 | low | pending | — |
| P3 | — | blocked (Windows) | — |

## Infrastructure State

- Version: v1.35.32+ (commit `cf3d699`)
- Tests: 2499 passed, 3 skipped
- Tree: clean

<next_action>
BUG-06: `project_type: unknown` on openmrs-core. Run `sourcecode --compact /path/to/openmrs-core` to confirm current output. Then find where `project_type` is classified — likely `repo_classifier.py` or `cli.py` compact output. openmrs-core has `pom.xml`, Spring MVC annotations, and 30 `@Transactional` boundaries — should classify as Spring.
</next_action>
