---
context: default
phase: null
task: null
total_tasks: 9
status: paused
last_updated: 2026-06-10T17:30:00Z
---

# Session 85 — Quality Fixes (8/9 complete)

<current_state>
Session 85 complete. v1.35.30 base (a17dcac) + P6/P7 (2f0513f) + P9 (29a6c7d).
8 of 9 tasks done. Only P3 remains — blocked on Windows.
</current_state>

<completed_work>

- **P1 DONE** — `impact` field injection: `_all_callers_from_rg` normalizes `injects`-edge FQNs via `_normalize_owner_fqn`. `@Autowired`, `@Inject`, `@Resource`, same-package all work.
- **P2 DONE** — All `M3FiltroSeguridad`/`saint-server` refs purged from 8 source files. `_CUSTOM_SECURITY_ANNOTATIONS` and `_CUSTOM_AOP_ANNOTATIONS` are now empty extension-point dicts.
- **P4 DONE** — `cache status/clear/warm` use `_resolve_repo_root()` that walks `.git` ancestry.
- **P5 DONE** — `cold-start --output/-o FILE` flag added.
- **P8 DONE** — `pr-impact --files` error explains expected format. `fix-bug` detects symptom-as-path misuse.
- **P6 DONE** — `transactional_boundaries.count` → `class_count` (compact view). `tx_boundaries_found` → `tx_annotation_count` (spring-audit metadata). `tx_stats.class_level` cross-references `class_count`. 3 tests updated.
- **P7 DONE** — `_TOOL_OUTPUT_RE` added to `summarizer._extract_first_useful_paragraph`. Rejects CVE ids, Trivy severity lines, scanner names (trivy/snyk/grype/syft), PURL identifiers.
- **P9 DONE** — `repo-ir --gzip` flag added (requires `--output`, auto-appends `.gz`, shows compressed+uncompressed KB). 10MB size warning for uncompressed file output. Error when `--gzip` without `--output`.
</completed_work>

<remaining_work>

- **P3** — Cache slow on Windows (cold ~8s, warm ~4s). `cache.py:810` `_atomic_write` — `Path.replace()` fails silently on locked files → CAS blobs never written → no dedup → slower. Fix: add `PermissionError` fallback (e.g. `shutil.copy2` + unlink pattern). Cannot validate without Windows.
</remaining_work>

<decisions_made>

- `count` → `class_count`: non-breaking rename, makes compact vs spring-audit semantics unambiguous for agents.
- `_TOOL_OUTPUT_RE` as separate class var (not merged into `_LICENSE_MARKETING_RE`): different noise category, easier to extend.
- `--gzip` auto-appends `.gz` to output path: prevents binary corruption of `.json` files.
- `--gzip` + stdout = explicit error: compressed binary on stdout breaks pipelines silently.
</decisions_made>

<blockers>

- P3 requires Windows to validate. Can inspect `_atomic_write` statically but fix is unverifiable on macOS.
</blockers>

## Required Reading

1. `src/sourcecode/cache.py:810` — `_atomic_write` (P3 investigation, Windows `Path.replace()` failure)

## Infrastructure State

- Tests: 2436 passed, 3 skipped, 0 new failures
- Version: v1.35.30, commits a17dcac → 2f0513f → 29a6c7d

<next_action>
P3 only. Read `_atomic_write` in `cache.py:810`. Add `PermissionError` try/except fallback using `shutil.copy2` + `unlink` pattern for Windows locked-file failure on `Path.replace()`.
</next_action>
