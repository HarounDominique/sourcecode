---
phase: 09-llm-output-quality
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - src/sourcecode/serializer.py
  - tests/test_integration_lqn.py
autonomous: true
requirements:
  - LQN-01
  - LQN-02
  - LQN-03
  - LQN-04
  - LQN-05
  - LQN-06

must_haves:
  truths:
    - "compact_view() incluye project_summary siempre"
    - "compact_view() incluye file_paths siempre"
    - "compact_view() incluye dependency_summary cuando sm.dependency_summary.requested es True"
    - "compact_view() NO incluye dependency_summary cuando dependency_summary es None o requested=False"
    - "file_tree_depth1 sigue presente en compact_view (backward compat)"
    - "Los 6 requisitos LQN-01..06 pasan en tests de integracion E2E"
    - "ruff check y mypy reportan 0 errores en archivos modificados de la Fase 9"
    - "Suite completa de pytest pasa sin regresiones"
  artifacts:
    - path: "src/sourcecode/serializer.py"
      provides: "compact_view() actualizado con project_summary, file_paths, dependency_summary"
    - path: "tests/test_integration_lqn.py"
      provides: "Tests de integracion LQN-01 a LQN-06 todos verdes"
  key_links:
    - from: "src/sourcecode/serializer.py compact_view()"
      to: "src/sourcecode/schema.py SourceMap.project_summary"
      via: "sm.project_summary (string ya generada por Plan 09-01)"
      pattern: "sm.project_summary"
    - from: "src/sourcecode/serializer.py compact_view()"
      to: "src/sourcecode/schema.py SourceMap.file_paths"
      via: "sm.file_paths (lista ya poblada por Plan 09-01)"
      pattern: "sm.file_paths"
    - from: "src/sourcecode/serializer.py compact_view()"
      to: "src/sourcecode/schema.py SourceMap.dependency_summary"
      via: "asdict(sm.dependency_summary) cuando requested=True"
      pattern: "dependency_summary"
---

<objective>
Este plan actualiza compact_view() en serializer.py para incluir project_summary, file_paths y
dependency_summary, y crea los tests de integracion end-to-end para LQN-01..06. Tambien ejecuta
el gate de calidad final: ruff + mypy sobre todos los archivos modificados de la Fase 9, y verifica
que la suite completa queda verde.

Purpose: Cierre de la Fase 9. Despues de este plan todos los requisitos LQN-01..06 estan implementados
y verificados.

Output:
- src/sourcecode/serializer.py (compact_view actualizado)
- tests/test_integration_lqn.py (NUEVO — 6+ tests de integracion)
</objective>

<execution_context>
@C:/Users/dominique.haroun_m3i/Downloads/proyecto/atlas-cli/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dominique.haroun_m3i/Downloads/proyecto/atlas-cli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-llm-output-quality/09-CONTEXT.md
@.planning/phases/09-llm-output-quality/09-01-SUMMARY.md
@.planning/phases/09-llm-output-quality/09-02-SUMMARY.md

@src/sourcecode/serializer.py
@src/sourcecode/schema.py
@src/sourcecode/cli.py
@tests/test_integration_docs.py
@tests/test_integration_dependencies.py
</context>

<interfaces>
<!-- Contratos de Planes 09-01 y 09-02 que este plan consume -->

De src/sourcecode/schema.py TRAS Planes 09-01 y 09-02 (SourceMap completo):
```python
@dataclass
class SourceMap:
    metadata: AnalysisMetadata
    file_tree: dict[str, Any]
    stacks: list[StackDetection]
    project_type: Optional[str]
    entry_points: list[EntryPoint]
    dependencies: list[DependencyRecord]
    dependency_summary: Optional[DependencySummary]
    module_graph: Optional[ModuleGraph]
    module_graph_summary: Optional[ModuleGraphSummary]
    docs: list[DocRecord]
    doc_summary: Optional[DocSummary]
    # Campos de Plan 09-01:
    file_paths: list[str]             # lista plana posix, siempre presente
    project_summary: Optional[str]    # string NL, siempre presente cuando hay stacks
    key_dependencies: list[DependencyRecord]  # top-15, solo con --dependencies

@dataclass
class DocRecord:
    symbol: str
    kind: str
    language: str
    path: str
    doc_text: Optional[str]
    signature: Optional[str]
    source: str
    importance: Literal["high", "medium", "low"]   # Nuevo de Plan 09-02
    workspace: Optional[str]

@dataclass
class DependencySummary:
    requested: bool    # True cuando --dependencies activo
    total_count: int
    direct_count: int
    transitive_count: int
    ecosystems: list[str]
    sources: list[str]
    limitations: list[str]
```

De src/sourcecode/serializer.py (compact_view actual — se actualiza aqui):
```python
def compact_view(sm: SourceMap) -> dict[str, Any]:
    # Actual: devuelve schema_version, project_type, stacks, entry_points, file_tree_depth1
    # NUEVO: tambien project_summary, file_paths, dependency_summary (conditional)
    ...
```

Patron para dependency_summary en compact_view:
```python
"dependency_summary": (
    asdict(sm.dependency_summary)
    if sm.dependency_summary is not None and sm.dependency_summary.requested
    else None
),
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: Actualizar compact_view() en serializer.py</name>
  <files>src/sourcecode/serializer.py</files>

  <read_first>
    - src/sourcecode/serializer.py — compact_view() actual (lineas ~51-75)
    - src/sourcecode/schema.py — SourceMap con campos nuevos de Planes 09-01/09-02
  </read_first>

  <behavior>
    Tests a añadir en el archivo de tests del serializer (o test_schema.py si no existe test_compact.py):
    - Test C1: compact_view(SourceMap(project_summary="API en Python.")) incluye "project_summary": "API en Python."
    - Test C2: compact_view(SourceMap()) incluye "project_summary": None (cuando es None)
    - Test C3: compact_view(SourceMap(file_paths=["src/a.py","src/b.py"])) incluye "file_paths": ["src/a.py","src/b.py"]
    - Test C4: compact_view(SourceMap(file_paths=[])) incluye "file_paths": []
    - Test C5: compact_view(SourceMap(dependency_summary=DependencySummary(requested=True, total_count=5))) incluye "dependency_summary" como dict
    - Test C6: compact_view(SourceMap(dependency_summary=None)) incluye "dependency_summary": None
    - Test C7: compact_view(SourceMap(dependency_summary=DependencySummary(requested=False))) incluye "dependency_summary": None
    - Test C8: compact_view(SourceMap()) incluye "file_tree_depth1" (backward compat)
    - Test C9: compact_view() NO incluye "dependencies" (la lista larga de DependencyRecords)
    - Test C10: compact_view() NO incluye "docs" ni "module_graph" (sin cambio del comportamiento anterior)
  </behavior>

  <action>
En src/sourcecode/serializer.py, reemplazar compact_view() con la version actualizada:

```python
def compact_view(sm: SourceMap) -> dict[str, Any]:
    """Proyeccion compacta del SourceMap (~500-700 tokens).

    Incluye: schema_version, project_type, stacks, entry_points,
    project_summary (siempre), file_paths (siempre),
    dependency_summary (cuando requested=True),
    file_tree_depth1 (backward compat).

    Excluye: dependencies (lista larga), docs, module_graph.
    """
    depth1: dict[str, Any] = {}
    for name, value in sm.file_tree.items():
        if isinstance(value, dict):
            depth1[name] = {}
        else:
            depth1[name] = None

    dep_summary_dict: Any = None
    if sm.dependency_summary is not None and sm.dependency_summary.requested:
        dep_summary_dict = asdict(sm.dependency_summary)

    return {
        "schema_version": sm.metadata.schema_version,
        "project_type": sm.project_type,
        "project_summary": sm.project_summary,
        "stacks": [asdict(stack) for stack in sm.stacks],
        "entry_points": [asdict(entry_point) for entry_point in sm.entry_points],
        "file_paths": sm.file_paths,
        "file_tree_depth1": depth1,
        "dependency_summary": dep_summary_dict,
    }
```

Escribir primero los tests C1..C10 (RED), luego actualizar compact_view (GREEN).
Verificar que la asercion de test_cli.py sobre --compact sigue pasando.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/ -k "compact" -x -q 2>&1 | tail -8</automated>
  </verify>

  <acceptance_criteria>
    - compact_view() incluye project_summary (siempre)
    - compact_view() incluye file_paths (siempre)
    - compact_view() incluye dependency_summary como dict cuando requested=True
    - compact_view() incluye dependency_summary=None cuando dependency_summary is None o requested=False
    - compact_view() sigue incluyendo file_tree_depth1 (backward compat)
    - compact_view() NO incluye dependencies[], docs[], module_graph
    - Tests de compact en test_cli.py y otros siguen pasando
  </acceptance_criteria>

  <done>pytest tests/ -k compact green; compact_view() actualizado</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: Crear tests/test_integration_lqn.py con LQN-01..06 E2E</name>
  <files>tests/test_integration_lqn.py</files>

  <read_first>
    - tests/test_integration_docs.py — ver patron CliRunner + fixture de proyectos de prueba
    - tests/test_integration_dependencies.py — ver patron de tests con --dependencies
    - tests/conftest.py — ver fixtures disponibles
  </read_first>

  <behavior>
    Tests de integracion usando typer CliRunner (igual que test_integration_docs.py):

    LQN-01 test: sourcecode . -> file_paths no-vacio, todos con "/" como separador
      - Invocar CLI sobre el propio proyecto del repositorio o un fixture
      - Parsear JSON output
      - assert "file_paths" in output_dict
      - assert len(output_dict["file_paths"]) > 0
      - assert all("/" in p or "." in p for p in output_dict["file_paths"])
      - assert not any("\\" in p for p in output_dict["file_paths"])

    LQN-02 test: sourcecode . -> project_summary no-None y no-vacia con stack detectado
      - assert "project_summary" in output_dict
      - assert isinstance(output_dict["project_summary"], str)
      - assert len(output_dict["project_summary"]) > 10
      - (El proyecto atlas-cli es Python, luego el resumen debe mencionar Python o el stack)

    LQN-03 test: sourcecode . --docs -> todos los DocRecords tienen importance en {high,medium,low}
      - assert all(rec["importance"] in {"high","medium","low"} for rec in output_dict["docs"])

    LQN-04 test: sourcecode . --docs -> ningun DocRecord tiene source="unavailable"
      - assert not any(rec["source"] == "unavailable" for rec in output_dict["docs"])

    LQN-05 test: sourcecode . --dependencies -> key_dependencies <= 15, todos scope!=transitive
      - assert len(output_dict["key_dependencies"]) <= 15
      - for dep in output_dict["key_dependencies"]:
            assert dep["scope"] != "transitive"
            assert dep["source"] in ("manifest", "lockfile")

    LQN-06 test A: sourcecode . --compact -> output contiene project_summary
      - Invocar con --compact, parsear JSON
      - assert "project_summary" in compact_dict
      - assert compact_dict["project_summary"] is not None

    LQN-06 test B: sourcecode . --compact --dependencies -> output contiene dependency_summary
      - Invocar con --compact --dependencies
      - assert "dependency_summary" in compact_dict
      - assert compact_dict["dependency_summary"] is not None
      - assert compact_dict["dependency_summary"]["requested"] is True

    LQN-06 test C: sourcecode . --compact (sin --dependencies) -> dependency_summary es None
      - assert compact_dict.get("dependency_summary") is None
  </behavior>

  <action>
Crear tests/test_integration_lqn.py con la siguiente estructura:

```python
"""Tests de integracion para LQN-01..06 — LLM Output Quality.

Cada test invoca el CLI sobre el propio directorio del proyecto via CliRunner
y verifica el contrato de los requisitos LQN.
"""
from __future__ import annotations

import json
from pathlib import Path

import pytest
from typer.testing import CliRunner

from sourcecode.cli import app

runner = CliRunner()
PROJECT_ROOT = str(Path(__file__).parent.parent)


def _invoke_json(*args: str) -> dict:
    """Invoca CLI y retorna el output como dict. Falla si exit_code != 0."""
    result = runner.invoke(app, [PROJECT_ROOT, *args])
    assert result.exit_code == 0, f"CLI falló: {result.output}"
    return json.loads(result.output)


# LQN-01 ...
# LQN-02 ...
# LQN-03 ...
# etc.
```

Implementar todos los tests usando el patron _invoke_json. Para LQN-03 y LQN-04 pasar "--docs".
Para LQN-05 pasar "--dependencies". Para LQN-06 pasar "--compact" (y "--compact --dependencies").

IMPORTANTE: Los tests que usan --docs o --dependencies pueden ser lentos; usar pytest.mark o
fixture de proyecto pequeno si es necesario. Para mantener velocidad, preferir el propio
directorio del repositorio como fixture (ya tiene archivos Python conocidos).

Asegurar que los nombres de test incluyen el ID del requisito (ej. def test_lqn01_file_paths).
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_integration_lqn.py -v 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - tests/test_integration_lqn.py existe con >=8 tests
    - test_lqn01_*: file_paths presente, no-vacio, con "/"
    - test_lqn02_*: project_summary presente, string con stack name
    - test_lqn03_*: con --docs, todos importance in {high,medium,low}
    - test_lqn04_*: con --docs, ningun source="unavailable" en docs[]
    - test_lqn05_*: con --dependencies, key_dependencies <= 15, todos scope!=transitive
    - test_lqn06_*: --compact incluye project_summary; --compact --dependencies incluye dependency_summary
    - Todos los tests pasan
  </acceptance_criteria>

  <done>pytest tests/test_integration_lqn.py green (>=8 tests, LQN-01..06 cubiertos)</done>
</task>

<task type="auto">
  <name>Tarea 3: Gate de calidad final — ruff + mypy + suite completa</name>
  <files>src/sourcecode/schema.py, src/sourcecode/serializer.py, src/sourcecode/summarizer.py, src/sourcecode/doc_analyzer.py, src/sourcecode/cli.py</files>

  <read_first>
    - .planning/phases/08-documentacion-extraida/08-04-SUMMARY.md — errores ruff/mypy
      tipicos de este codebase para anticipar patrones (SIM102, arg-type, etc.)
  </read_first>

  <action>
PASO 1 — Ejecutar ruff check sobre archivos modificados de la Fase 9:
```bash
cd "C:/Users/dominique.haroun_m3i/Downloads/proyecto/atlas-cli"
.venv/bin/python -m ruff check src/sourcecode/schema.py src/sourcecode/serializer.py src/sourcecode/summarizer.py src/sourcecode/doc_analyzer.py src/sourcecode/cli.py
```

Por cada error que reporte:
- E501 (line too long): reformatear la linea afectada
- I001 (import sort): reordenar imports en orden alfabetico
- SIM102 (nested if): combinar con `and`
- SIM114 (elif with same result): combinar con `or`
- Otros: aplicar el fix sugerido por ruff o manual

Si ruff --fix puede autoarreglar, ejecutar:
```bash
.venv/bin/python -m ruff check --fix src/sourcecode/summarizer.py src/sourcecode/serializer.py
```

PASO 2 — Ejecutar mypy sobre archivos modificados:
```bash
.venv/bin/python -m mypy src/sourcecode/schema.py src/sourcecode/serializer.py src/sourcecode/summarizer.py src/sourcecode/doc_analyzer.py src/sourcecode/cli.py --ignore-missing-imports
```

Por cada error de mypy:
- arg-type: añadir cast o anotacion de tipo explicita
- return-value: verificar la firma del metodo
- attr-defined: verificar que el campo existe en el dataclass
- Literal incompatibility: usar cast() o Union explicito

Tipicamente: _infer_importance retorna Literal, verificar que la firma declara -> Literal["high","medium","low"]
y que el tipo de retorno esta importado.

PASO 3 — Ejecutar suite completa:
```bash
.venv/bin/python -m pytest tests/ -q 2>&1 | tail -10
```

Si hay fallos nuevos: investigar, corregir y re-ejecutar.
No ignorar fallos con skip. El objetivo es 0 errores nuevos (los skips pre-existentes son OK).

PASO 4 — Verificacion final del contrato LQN completo:
```bash
.venv/bin/python -m sourcecode . --format json 2>/dev/null | .venv/bin/python -c "
import sys, json
d = json.load(sys.stdin)
checks = [
    ('LQN-01 file_paths', bool(d.get('file_paths')) and all('/' in p or '.' in p for p in d['file_paths'])),
    ('LQN-02 project_summary', bool(d.get('project_summary'))),
    ('LQN-06 compact project_summary', True),  # verificado en LQN tests
]
for name, ok in checks:
    print(f\"{'OK' if ok else 'FAIL'}: {name}\")
"
```
  </action>

  <verify>
    <automated>.venv/bin/python -m ruff check src/sourcecode/schema.py src/sourcecode/serializer.py src/sourcecode/summarizer.py src/sourcecode/doc_analyzer.py src/sourcecode/cli.py && echo "ruff: OK" && .venv/bin/python -m mypy src/sourcecode/schema.py src/sourcecode/serializer.py src/sourcecode/summarizer.py src/sourcecode/doc_analyzer.py src/sourcecode/cli.py --ignore-missing-imports && echo "mypy: OK"</automated>
  </verify>

  <acceptance_criteria>
    - ruff check retorna 0 errores en todos los archivos de la Fase 9
    - mypy retorna 0 errores en todos los archivos de la Fase 9
    - pytest tests/ pasa con 0 nuevos fallos (skips pre-existentes son OK)
    - pytest tests/test_integration_lqn.py pasa con todos los LQN-01..06 verdes
  </acceptance_criteria>

  <done>ruff OK, mypy OK, suite completa green, LQN-01..06 verificados</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compact_view → dependency_summary | Solo se incluye cuando requested=True (activo por --dependencies del usuario) |
| test_integration_lqn → CLI | Tests usan CliRunner (subprocess simulado), no acceso real a filesystem externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Information Disclosure | compact_view + dependency_summary | mitigate | dependency_summary solo se incluye cuando sm.dependency_summary.requested is True, garantizando que solo aparece cuando el usuario activo --dependencies explicitamente. Sin leak implicito de informacion de deps cuando no se solicitan. |
| T-09-03-02 | Information Disclosure | file_paths en compact_view | accept | file_paths ya esta en el output no-compact. En compact es una proyeccion de datos ya expuestos. El SecretRedactor ya filtro el file_tree origen. Riesgo residual aceptable. |
| T-09-03-03 | Tampering | compact_view key ordering | accept | El orden de claves en el dict retornado es determinista (literal Python dict). No hay riesgo de manipulacion desde el exterior. |
</threat_model>

<verification>
```bash
cd "C:/Users/dominique.haroun_m3i/Downloads/proyecto/atlas-cli"

# 1. compact_view tests
.venv/bin/python -m pytest tests/ -k "compact" -v 2>&1 | tail -15

# 2. Suite LQN completa
.venv/bin/python -m pytest tests/test_integration_lqn.py -v 2>&1 | tail -20

# 3. Linting y tipado
.venv/bin/python -m ruff check src/sourcecode/schema.py src/sourcecode/serializer.py src/sourcecode/summarizer.py src/sourcecode/doc_analyzer.py src/sourcecode/cli.py
.venv/bin/python -m mypy src/sourcecode/schema.py src/sourcecode/serializer.py src/sourcecode/summarizer.py src/sourcecode/doc_analyzer.py src/sourcecode/cli.py --ignore-missing-imports

# 4. Suite completa
.venv/bin/python -m pytest tests/ -q 2>&1 | tail -8

# 5. Verificacion E2E del contrato LQN-01..06
.venv/bin/python -m sourcecode . --format json 2>/dev/null | .venv/bin/python -c "
import sys, json
d = json.load(sys.stdin)
# LQN-01
fps = d.get('file_paths', [])
assert fps and all('/' in p or '.' in p for p in fps) and not any('\\\\' in p for p in fps), 'LQN-01 FAIL'
# LQN-02
ps = d.get('project_summary', '')
assert ps and len(ps) > 10, 'LQN-02 FAIL'
print('LQN-01 OK: file_paths=' + str(len(fps)))
print('LQN-02 OK: project_summary=' + ps[:80])
"

.venv/bin/python -m sourcecode . --compact --format json 2>/dev/null | .venv/bin/python -c "
import sys, json
d = json.load(sys.stdin)
assert 'project_summary' in d, 'LQN-06 compact project_summary FAIL'
assert 'file_paths' in d, 'LQN-06 compact file_paths FAIL'
assert 'file_tree_depth1' in d, 'backward compat FAIL'
assert d.get('dependency_summary') is None, 'LQN-06 dep_summary sin --deps deberia ser None'
print('LQN-06 compact (sin --dependencies) OK')
"

.venv/bin/python -m sourcecode . --compact --dependencies --format json 2>/dev/null | .venv/bin/python -c "
import sys, json
d = json.load(sys.stdin)
assert 'dependency_summary' in d and d['dependency_summary'] is not None, 'LQN-06 dep_summary FAIL'
assert d['dependency_summary']['requested'] is True, 'LQN-06 dep_summary.requested FAIL'
print('LQN-06 compact --dependencies OK')
"
```
</verification>

<success_criteria>
- [ ] compact_view() incluye project_summary (siempre)
- [ ] compact_view() incluye file_paths (siempre)
- [ ] compact_view() incluye dependency_summary dict cuando requested=True
- [ ] compact_view() incluye dependency_summary=None cuando no activo
- [ ] compact_view() sigue incluyendo file_tree_depth1
- [ ] tests/test_integration_lqn.py existe con >=8 tests, todos verdes
- [ ] LQN-01: file_paths no-vacio, todos forward-slash
- [ ] LQN-02: project_summary presente y no-vacia
- [ ] LQN-03: todos DocRecords tienen importance en {high,medium,low}
- [ ] LQN-04: ningun DocRecord tiene source="unavailable" en docs[]
- [ ] LQN-05: key_dependencies <=15, todos scope!=transitive
- [ ] LQN-06: --compact incluye project_summary; --compact --dependencies incluye dependency_summary
- [ ] ruff check: 0 errores en archivos de Fase 9
- [ ] mypy: 0 errores en archivos de Fase 9
- [ ] pytest tests/ green (0 nuevas regresiones)
</success_criteria>

<output>
Despues de completar este plan, crear:
`.planning/phases/09-llm-output-quality/09-03-SUMMARY.md`

El SUMMARY debe incluir: cambios en compact_view(), tests LQN creados, resultados de ruff/mypy,
metricas de la suite final, y confirmacion de que LQN-01..06 estan todos verdes.

Este plan es el cierre de la Fase 9 — el SUMMARY debe confirmar que todos los success criteria
de la fase estan satisfechos.
</output>
