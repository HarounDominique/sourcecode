---
phase: 09-llm-output-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sourcecode/schema.py
  - src/sourcecode/summarizer.py
  - src/sourcecode/cli.py
  - tests/test_schema.py
  - tests/test_summarizer.py
autonomous: true
requirements:
  - LQN-01
  - LQN-02
  - LQN-05

must_haves:
  truths:
    - "SourceMap tiene campos file_paths, project_summary y key_dependencies con defaults vacios/None"
    - "ProjectSummarizer.generate() retorna una string no-vacia cuando hay stacks detectados"
    - "cli.py puebla file_paths con paths posix derivados de file_tree tras construir el SourceMap"
    - "cli.py puebla key_dependencies filtrando scope!=transitive y source in {manifest,lockfile}, cap 15, solo con --dependencies"
    - "cli.py llama ProjectSummarizer().generate(sm) y asigna sm.project_summary antes de serializar"
  artifacts:
    - path: "src/sourcecode/summarizer.py"
      provides: "ProjectSummarizer con metodo generate(sm: SourceMap) -> str"
      exports: ["ProjectSummarizer"]
    - path: "tests/test_summarizer.py"
      provides: "Tests unitarios para todas las ramas de template de ProjectSummarizer"
    - path: "tests/test_schema.py"
      provides: "Verificacion de nuevos campos en SourceMap"
  key_links:
    - from: "src/sourcecode/cli.py"
      to: "src/sourcecode/summarizer.py"
      via: "import lazy dentro de main() + llamada ProjectSummarizer().generate(sm)"
      pattern: "ProjectSummarizer"
    - from: "src/sourcecode/cli.py"
      to: "src/sourcecode/tree_utils.flatten_file_tree"
      via: "llamada con file_tree para poblar sm.file_paths"
      pattern: "flatten_file_tree"
---

<objective>
Este plan añade tres campos nuevos a SourceMap (file_paths, project_summary, key_dependencies),
crea el modulo summarizer.py con ProjectSummarizer, y actualiza cli.py para poblar los tres campos.

Purpose: Sentar la base de datos del output LLM-friendly. Los planes 09-02 y 09-03 dependen de
que estos campos existan en SourceMap con defaults backward-compatible.

Output:
- src/sourcecode/summarizer.py (NUEVO)
- src/sourcecode/schema.py (ampliado con 3 campos)
- src/sourcecode/cli.py (wiring de los 3 campos)
- tests/test_schema.py (actualizado)
- tests/test_summarizer.py (NUEVO)
</objective>

<execution_context>
@C:/Users/dominique.haroun_m3i/Downloads/proyecto/atlas-cli/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dominique.haroun_m3i/Downloads/proyecto/atlas-cli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-llm-output-quality/09-CONTEXT.md

@src/sourcecode/schema.py
@src/sourcecode/cli.py
@src/sourcecode/tree_utils.py
@tests/test_schema.py
</context>

<interfaces>
<!-- Contratos actuales del codebase que este plan consume y extiende -->

De src/sourcecode/schema.py (campos actuales de SourceMap — se amplian aqui):
```python
@dataclass
class SourceMap:
    metadata: AnalysisMetadata = field(default_factory=AnalysisMetadata)
    file_tree: dict[str, Any] = field(default_factory=dict)
    stacks: list[StackDetection] = field(default_factory=list)
    project_type: Optional[str] = None
    entry_points: list[EntryPoint] = field(default_factory=list)
    dependencies: list[DependencyRecord] = field(default_factory=list)
    dependency_summary: Optional[DependencySummary] = None
    module_graph: Optional[ModuleGraph] = None
    module_graph_summary: Optional[ModuleGraphSummary] = None
    docs: list[DocRecord] = field(default_factory=list)
    doc_summary: Optional[DocSummary] = None
    # NUEVOS (este plan):
    # file_paths: list[str] = field(default_factory=list)
    # project_summary: Optional[str] = None
    # key_dependencies: list[DependencyRecord] = field(default_factory=list)
```

De src/sourcecode/schema.py (DependencyRecord — usado para key_dependencies):
```python
@dataclass
class DependencyRecord:
    name: str
    ecosystem: str
    scope: str = "direct"          # direct | dev | optional | peer | transitive
    declared_version: Optional[str] = None
    resolved_version: Optional[str] = None
    source: str = "manifest"       # manifest | lockfile | tooling
    parent: Optional[str] = None
    manifest_path: Optional[str] = None
    workspace: Optional[str] = None
```

De src/sourcecode/tree_utils.py (flatten_file_tree — ya importado en doc_analyzer):
```python
def flatten_file_tree(file_tree: dict[str, Any], prefix: str = "") -> list[str]:
    """Aplana el file_tree jerarquico a paths relativos."""
    paths: list[str] = []
    for name, value in file_tree.items():
        current = f"{prefix}/{name}" if prefix else name
        paths.append(current)
        if isinstance(value, dict):
            paths.extend(flatten_file_tree(value, current))
    return paths
```

De src/sourcecode/schema.py (StackDetection — usado en ProjectSummarizer):
```python
@dataclass
class StackDetection:
    stack: str
    detection_method: Literal["manifest", "lockfile", "heuristic"] = "manifest"
    confidence: Literal["high", "medium", "low"] = "high"
    frameworks: list[FrameworkDetection] = field(default_factory=list)
    package_manager: Optional[str] = None
    manifests: list[str] = field(default_factory=list)
    primary: bool = False
    root: Optional[str] = None
    workspace: Optional[str] = None
    signals: list[str] = field(default_factory=list)

@dataclass
class DependencySummary:
    requested: bool = False
    total_count: int = 0
    direct_count: int = 0
    transitive_count: int = 0
    ecosystems: list[str] = field(default_factory=list)
    sources: list[str] = field(default_factory=list)
    limitations: list[str] = field(default_factory=list)
```

De src/sourcecode/cli.py (punto de insercion en main(), despues de construir sm):
```python
    sm = SourceMap(
        metadata=metadata,
        file_tree=file_tree,
        stacks=stacks,
        project_type=project_type,
        entry_points=entry_points,
        dependencies=dependency_records,
        dependency_summary=dependency_summary,
        module_graph=module_graph,
        module_graph_summary=...,
        docs=doc_records,
        doc_summary=doc_summary,
    )
    # INSERTAR AQUI: poblar file_paths, key_dependencies, project_summary
    # antes del bloque "if compact:"
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: Ampliar schema.py con file_paths, project_summary y key_dependencies</name>
  <files>src/sourcecode/schema.py, tests/test_schema.py</files>

  <read_first>
    - src/sourcecode/schema.py — ver SourceMap completo y patron de campos existentes
    - tests/test_schema.py — ver tests actuales para seguir el patron
  </read_first>

  <behavior>
    - Test 1: SourceMap() sin args tiene file_paths=[], project_summary=None, key_dependencies=[]
    - Test 2: SourceMap(file_paths=["src/main.py", "src/utils.py"]) persiste la lista
    - Test 3: SourceMap(project_summary="API en Python (FastAPI).") persiste la string
    - Test 4: SourceMap(key_dependencies=[DependencyRecord(name="fastapi", ecosystem="python")]) persiste la lista
    - Test 5: dataclasses.asdict(SourceMap()) incluye claves "file_paths", "project_summary", "key_dependencies"
    - Test 6: SourceMap existente sin pasar los campos nuevos sigue funcionando (backward compat)
  </behavior>

  <action>
En src/sourcecode/schema.py, en la clase SourceMap, anadir los tres campos DESPUES de doc_summary
(al final de la clase, para no romper el orden historico de campos existentes):

```python
    # Phase 9: LLM Output Quality
    file_paths: list[str] = field(default_factory=list)
    project_summary: Optional[str] = None
    key_dependencies: list[DependencyRecord] = field(default_factory=list)
```

En tests/test_schema.py, escribir primero los tests de comportamiento (RED), luego verificar
que pasan con el campo añadido (GREEN). Usar la clase SourceMap directamente con dataclasses.asdict.
Seguir el patron del test_schema.py existente para no duplicar imports.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_schema.py -x -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - SourceMap() inicializa con file_paths=[], project_summary=None, key_dependencies=[]
    - asdict(SourceMap()) contiene las tres nuevas claves
    - Tests existentes en test_schema.py siguen pasando (sin regresion)
  </acceptance_criteria>

  <done>pytest tests/test_schema.py green; schema.py tiene los 3 campos nuevos con defaults</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: Crear summarizer.py con ProjectSummarizer</name>
  <files>src/sourcecode/summarizer.py, tests/test_summarizer.py</files>

  <read_first>
    - src/sourcecode/schema.py — SourceMap, StackDetection, EntryPoint, DependencySummary
    - src/sourcecode/doc_analyzer.py — ver como se estructura una clase analizadora (patron a seguir)
  </read_first>

  <behavior>
    - Test 1: SourceMap vacio (sin stacks) -> generate() retorna "Proyecto sin stack detectado." (fallback graceful)
    - Test 2: SourceMap con stack python primary=True, sin frameworks ni deps -> "Proyecto Python. Sin dependencias detectadas."
    - Test 3: SourceMap con stack python, frameworks=[FastAPI], dependency_summary.total_count=5, ecosystems=["python"] -> contiene "Python", "FastAPI", "5 dependencias", "python"
    - Test 4: SourceMap con project_type="cli", entry_points=[EntryPoint(path="src/cli.py",stack="python",kind="entry")] -> contiene "cli" o "CLI", "src/cli.py"
    - Test 5: SourceMap con project_type="monorepo", stacks con 2 workspaces distintos -> contiene "monorepo" o "Monorepo"
    - Test 6: SourceMap con 2 stacks (python + nodejs), python primary=True -> python aparece primero en el resumen
    - Test 7: generate() retorna una string, nunca lanza excepcion con cualquier SourceMap valido
    - Test 8: entry_points con mas de 3 paths -> el resumen no lista mas de 3 (evitar linea muy larga)
  </behavior>

  <action>
Crear src/sourcecode/summarizer.py con la siguiente estructura:

```python
"""Generador deterministico de resumen en lenguaje natural del proyecto.

Sin llamadas a API — solo templates aplicados sobre SourceMap.
"""
from __future__ import annotations

from sourcecode.schema import SourceMap


class ProjectSummarizer:
    """Genera project_summary: string NL deterministica desde SourceMap."""

    def generate(self, sm: SourceMap) -> str:
        """Retorna descripcion NL del proyecto. Nunca lanza excepcion."""
        try:
            return self._build_summary(sm)
        except Exception:
            return "Proyecto analizado."

    def _build_summary(self, sm: SourceMap) -> str:
        # Determinar stack primario
        primary_stacks = [s for s in sm.stacks if s.primary]
        all_stacks = primary_stacks if primary_stacks else sm.stacks

        if not all_stacks:
            return "Proyecto sin stack detectado."

        # Tipo de proyecto
        project_type = sm.project_type or "Proyecto"
        type_label = {
            "webapp": "Aplicacion web",
            "api": "API",
            "library": "Libreria",
            "cli": "CLI",
            "monorepo": "Monorepo",
            "fullstack": "Proyecto fullstack",
            "unknown": "Proyecto",
        }.get(project_type, project_type.capitalize())

        # Stacks y frameworks
        primary = all_stacks[0]
        stack_name = primary.stack.capitalize()
        frameworks = [f.name for f in primary.frameworks]
        fw_part = f" ({', '.join(frameworks[:3])})" if frameworks else ""

        # Entry points (max 3)
        ep_paths = [ep.path for ep in sm.entry_points[:3]]
        ep_part = f" Entry points: {', '.join(ep_paths)}." if ep_paths else ""

        # Dependencias
        dep_part = ""
        if sm.dependency_summary and sm.dependency_summary.total_count > 0:
            ds = sm.dependency_summary
            ecosystems = ", ".join(ds.ecosystems[:3])
            dep_part = f" {ds.total_count} dependencias ({ecosystems})."
        elif sm.dependency_summary is None:
            dep_part = ""
        else:
            dep_part = " Sin dependencias detectadas."

        # Monorepo: variante especial
        if project_type == "monorepo":
            stacks_desc = ", ".join(sorted({s.stack.capitalize() for s in sm.stacks}))
            n_ws = len({s.workspace for s in sm.stacks if s.workspace})
            ws_part = f" con {n_ws} workspaces" if n_ws > 0 else ""
            return f"Monorepo{ws_part} en {stacks_desc}.{ep_part}{dep_part}"

        return f"{type_label} en {stack_name}{fw_part}.{ep_part}{dep_part}"
```

Crear tests/test_summarizer.py con los 8 casos de comportamiento definidos.
Ejecutar primero (RED: falla porque summarizer.py no existe aun), luego crear el modulo (GREEN).
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_summarizer.py -x -q 2>&1 | tail -8</automated>
  </verify>

  <acceptance_criteria>
    - Los 8 tests del comportamiento pasan
    - generate() nunca lanza excepcion en ninguna rama
    - El resumen de un proyecto FastAPI contiene "Python", "FastAPI" y cuenta de deps cuando hay dependency_summary
    - La variante monorepo contiene "Monorepo"
  </acceptance_criteria>

  <done>pytest tests/test_summarizer.py green (8+ tests)</done>
</task>

<task type="auto">
  <name>Tarea 3: Wiring en cli.py — poblar file_paths, key_dependencies y project_summary</name>
  <files>src/sourcecode/cli.py</files>

  <read_first>
    - src/sourcecode/cli.py — ver el bloque de construccion de sm (lineas ~358-371) y el bloque "if compact:" (lineas ~374+)
    - src/sourcecode/tree_utils.py — confirmar firma de flatten_file_tree
  </read_first>

  <action>
En src/sourcecode/cli.py, en la funcion main(), despues de construir `sm = SourceMap(...)` y
ANTES del bloque `if compact:`, insertar el bloque de wiring de los tres campos nuevos:

PASO 1 — Importar flatten_file_tree (ya esta importado en doc_analyzer; en cli.py anadir
al bloque de imports lazy dentro de main()):
```python
    from sourcecode.tree_utils import flatten_file_tree
    from sourcecode.summarizer import ProjectSummarizer
```

PASO 2 — Poblar file_paths con paths posix:
```python
    # LQN-01: lista plana de paths del file_tree con separador forward-slash
    sm.file_paths = [
        p.replace("\\", "/") for p in flatten_file_tree(sm.file_tree)
    ]
```
Nota: flatten_file_tree ya usa "/" al construir paths con f"{prefix}/{name}",
pero el replace es defensa adicional en Windows.

PASO 3 — Poblar key_dependencies (solo con --dependencies activo):
```python
    # LQN-05: top-15 dependencias directas de manifest/lockfile
    if dependency_analyzer is not None:
        primary_ecosystem = sm.stacks[0].stack if sm.stacks else ""
        direct_deps = [
            d for d in sm.dependencies
            if d.scope != "transitive" and d.source in {"manifest", "lockfile"}
        ]
        def _dep_sort_key(d: Any) -> tuple[int, str]:
            return (0 if d.ecosystem == primary_ecosystem else 1, d.name.lower())
        sm.key_dependencies = sorted(direct_deps, key=_dep_sort_key)[:15]
```

PASO 4 — Generar project_summary:
```python
    # LQN-02: resumen NL deterministico
    sm.project_summary = ProjectSummarizer().generate(sm)
```

El bloque completo se inserta entre la construccion de `sm` y el `if compact:`.
No modificar ninguna otra parte de cli.py.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_cli.py tests/test_schema.py tests/test_summarizer.py -x -q 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - pytest tests/test_cli.py pasa sin regresion
    - .venv/bin/python -m sourcecode . (en el directorio del proyecto) incluye "file_paths" en JSON
    - .venv/bin/python -m sourcecode . incluye "project_summary" con string no-vacia
    - .venv/bin/python -m sourcecode . --dependencies incluye "key_dependencies" como lista
    - .venv/bin/python -m sourcecode . (sin --dependencies) tiene key_dependencies=[]
  </acceptance_criteria>

  <done>cli.py popula los 3 campos nuevos; pytest tests/test_cli.py green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file_tree → file_paths | Los paths provienen del scanner filtrado; no de input de usuario directo |
| SourceMap.stacks → project_summary | El resumen se genera desde datos ya detectados estaticamente; sin interpolacion de strings de usuario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Information Disclosure | flatten_file_tree → file_paths | mitigate | file_paths deriva del mismo file_tree ya filtrado por filter_sensitive_files() y SecretRedactor; no expone paths adicionales. Los archivos .env ya son excluidos del file_tree antes de flatten. |
| T-09-01-02 | Information Disclosure | project_summary template | mitigate | ProjectSummarizer usa solo campos estructurales del SourceMap (stack, type, entry_points, dep counts). No incluye contenido de archivos ni valores de dependencias. La string resultante no puede contener secretos. |
| T-09-01-03 | Denial of Service | key_dependencies filter loop | accept | El loop itera sobre sm.dependencies que ya tiene cap implicito del DependencyAnalyzer. El slicing [:15] es O(1). Riesgo bajo. |
</threat_model>

<verification>
```bash
# 1. Tests unitarios del plan
cd "C:/Users/dominique.haroun_m3i/Downloads/proyecto/atlas-cli"
.venv/bin/python -m pytest tests/test_schema.py tests/test_summarizer.py tests/test_cli.py -x -q

# 2. Verificacion de campos en output real
.venv/bin/python -m sourcecode . --format json 2>/dev/null | .venv/bin/python -c "
import sys, json
d = json.load(sys.stdin)
assert 'file_paths' in d, 'FALTA file_paths'
assert isinstance(d['file_paths'], list) and len(d['file_paths']) > 0, 'file_paths vacio'
assert all('/' in p or '.' in p for p in d['file_paths'][:5]), 'paths invalidos'
assert 'project_summary' in d, 'FALTA project_summary'
assert isinstance(d['project_summary'], str) and len(d['project_summary']) > 5, 'project_summary invalido'
assert 'key_dependencies' in d, 'FALTA key_dependencies'
print('OK: file_paths=' + str(len(d['file_paths'])) + ', summary=' + d['project_summary'][:60])
"

# 3. key_dependencies con --dependencies
.venv/bin/python -m sourcecode . --dependencies --format json 2>/dev/null | .venv/bin/python -c "
import sys, json
d = json.load(sys.stdin)
kd = d.get('key_dependencies', [])
assert len(kd) <= 15, f'key_dependencies excede 15: {len(kd)}'
for dep in kd:
    assert dep['scope'] != 'transitive', f'dep transitiva en key_dependencies: {dep[\"name\"]}'
    assert dep['source'] in ('manifest', 'lockfile'), f'source invalida: {dep[\"source\"]}'
print('OK: key_dependencies=' + str(len(kd)))
"

# 4. Suite completa sin regresion
.venv/bin/python -m pytest tests/ -x -q 2>&1 | tail -5
```
</verification>

<success_criteria>
- [ ] SourceMap tiene file_paths: list[str], project_summary: Optional[str], key_dependencies: list[DependencyRecord] con defaults backward-compat
- [ ] src/sourcecode/summarizer.py existe con ProjectSummarizer.generate()
- [ ] tests/test_summarizer.py existe con >=8 tests, todos verdes
- [ ] cli.py popula los 3 campos despues de construir sm
- [ ] file_paths contiene paths con separador "/"
- [ ] key_dependencies <= 15 elementos, todos scope!=transitive, source in {manifest,lockfile}
- [ ] project_summary es string no-vacia en proyectos con stacks detectados
- [ ] pytest tests/test_schema.py tests/test_summarizer.py tests/test_cli.py green
- [ ] Suite completa sin nuevas regresiones
</success_criteria>

<output>
Despues de completar este plan, crear:
`.planning/phases/09-llm-output-quality/09-01-SUMMARY.md`

El SUMMARY debe incluir: campos añadidos a SourceMap, plantillas implementadas en ProjectSummarizer,
punto exacto de insercion en cli.py, metricas de tests, y cualquier desviacion del plan.
</output>
